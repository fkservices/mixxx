# Discovery decisions: three rounds of twenty

> Autonomously AI-generated planning record at the user's request.

All 60 questions are resolved for planning: 26 direct user answers, 2 choices from the user’s standing preference, and 32 assistant defaults authorized by “answer all the other questions your self.” Defaults are editable and are not presented as individual user answers. The [structured record](DISCOVERY.json) preserves exact wording and provenance. Earlier answers may note what was open at the time; later rounds and the delegated defaults resolve those choices. User decisions take priority.

## Round 1 — Playlist workflow and musical behavior

| ID | Question | Decision | Source |
| --- | --- | --- | --- |
| R1-Q01 | Where do you primarily expect to use the AI DJ? | Both private listening and live DJ sessions | User |
| R1-Q02 | What kind of music will a typical playlist contain? | Support open-format playlists across genres, tempos and eras. Use BPM, harmonic compatibility and lyrical/vocal matching in selection and transitions. Exact lyrical matching behavior and data source remain to be clarified. | User |
| R1-Q03 | How many songs should a typical playlist contain? | Typical playlists contain at least 10 songs. No upper size limit has been specified; this does not exclude smaller playlists or establish unlimited capacity. | User |
| R1-Q04 | How long will a typical performed set last? | Support short sets of approximately 10 minutes and sessions lasting multiple hours. No precise maximum duration is specified. How short set durations interact with playing all playlist entries remains to be clarified. | User |
| R1-Q05 | When you say the AI should play every song, how much of each song should it play? | AI selects musically appropriate portions of tracks. Set duration supports automatic AI selection, a user-defined time limit, or a user-defined duration range. Fit section and transition choices to these constraints while accounting for every playlist entry; handling infeasible combinations remains for R1-Q12. | User |
| R1-Q06 | What mixing style should the AI use by default? | Choose mixing style automatically from the music by default; provide a user-selectable style override. | User |
| R1-Q07 | How much should the AI alter a song’s tempo to make a transition work? | Prefer small tempo changes; normally allow up to moderate changes when useful. Expose user-configurable tempo limits. Numerical default thresholds are not yet specified. | User |
| R1-Q08 | How should the AI choose the energy flow of a set? | Plan set energy automatically by default. Provide selectable energy presets and a user-editable custom energy curve as part of the set-planning UI. | User |
| R1-Q09 | After choosing a playlist in AI Only mode, what should happen before music starts? | Offer selectable review-first and automatic-start workflows in AI Only mode. Automatic start applies to an explicitly initiated set workflow; it does not override latched disarm or authorize startup/reconnect replay. | User |
| R1-Q10 | How should opening and closing tracks be chosen? | AI chooses opening and closing tracks by default; user can pin either or both. | User standing preference |
| R1-Q11 | Once a set starts, how freely may AI reorder the remaining songs? | AI may adapt the remaining order during the set while respecting user pins and constraints; active-transition changes remain subject to mode/ownership rules. | User standing preference |
| R1-Q12 | If a requested set duration is too short to include every song properly, what should happen? | When the time constraint cannot fit meaningful participation by every playlist entry, show the conflict and obtain the user choice before starting. Additional requirement: use existing Mixxx cue/hotcue markers and MIDI controller mappings in planning and execution. | User |
| R1-Q13 | If the same track appears twice in the supplied playlist, how should we handle it? | Keep repeated playlist occurrences and show a non-blocking duplicate notice. Prefer musically suitable sections not previously played, using playback-section history; this is best effort rather than an absolute ban on repeated sections. Whether history should extend beyond the current session remains to be clarified. | User |
| R1-Q14 | If some playlist tracks are missing or cannot be played, what should happen before starting? | Automatically skip missing/unreadable/unplayable entries and advance to the next playable song. Apply to preflight and confirmed load failures during the set. Preserve skipped occurrence and reason in history; do not mark it played or silently delete it from the source playlist. If no playable entries remain, end/report the condition rather than retrying indefinitely. | User |
| R1-Q15 | May the AI introduce songs that were not in the supplied playlist? | Provide a per-set music-pool setting: restrict AI selection to the supplied playlist, or allow free selection/additions from the local Mixxx library. Keep this independent of AI Only, B2B and Playlist Only performance modes. Original occurrence accounting and selected duration policy still apply. Default music-pool choice is not specified. | User |
| R1-Q16 | If you edit the original playlist in Mixxx while our set is running, how should the app respond? | Provide a playlist-sync setting. Live sync automatically applies original Mixxx playlist edits to the remaining set while preserving the active transition; snapshot mode keeps the starting playlist until the user manually refreshes. Record source/plan revisions and edits in session history. Default setting is not specified. | User |
| R1-Q17 | Where should an arranged set be saved? | Always support saving the arranged set in the app. Provide selectable behaviors: optionally save a new Mixxx playlist on request; automatically save a new Mixxx playlist as well; or keep the set in the app with manual file export. Preserve the original playlist. Default save behavior is not specified. | User |
| R1-Q18 | How should track preparation work before a set? | Reuse existing valid analysis and prepare the first few tracks, then analyze missing information ahead during playback. Offer a prepare-everything-first setting and user overrides. The exact scope of overrides beyond preparation strategy (such as forced reanalysis or manual feature edits) remains to be specified. | User |
| R1-Q19 | How should the AI learn your musical preferences over time? | Learn preferences over time from explicit ratings and saved settings plus set-list edits and manual mixing interventions. Keep learning local and retain context/provenance; a single temporary gesture should not automatically become a permanent global rule. Learning controls and correction/reset behavior remain for later discovery. | User |
| R1-Q20 | Which musical outcome matters most when judging the first build? | Balance smooth natural transitions, an enjoyable full-set journey and creative experimentation. The result should be fun to hear without unintended tempo/beat mismatch or harmonic clashes. Use listening review and technical timing/compatibility checks; creativity is not permission for poor execution. | User |

## Round 2 — Modes, physical controllers and B2B handover

| ID | Question | Decision | Source |
| --- | --- | --- | --- |
| R2-Q01 | Which physical MIDI controller will we use for the first real B2B test? | User has a basic MIDI controller and mentions Hercules and Reloop Buddy. Reloop Buddy is a named candidate; exact Hercules model, complete device inventory and primary test controller remain unconfirmed. No Mixxx compatibility is yet verified. | User |
| R2-Q02 | Which performance mode should be selected when the app opens, with performance automation still disarmed? | Persist the last selected performance mode across app launches. Do not persist armed state or replay old actions. First-launch mode was not specified in this answer; delegated R2-Q20 later selects B2B disarmed. | User |
| R2-Q03 | In B2B, what default quiet period should follow the last gesture before AI becomes eligible to retake a released control? The setting remains user-configurable. | Default configurable quiet period is 3 seconds. In B2B a manual adjustment owns that item for the rest of the current transition. AI cannot reclaim merely because the timer expires; reclaim is allowed at a later transition start only after input/touch release, quiet expiry and fresh reconciliation. If still touched or not quiet at that start, retain the hold. Start any new action from the actual user value. | User |
| R2-Q04 | In AI Only, when should AI restore its plan after an ordinary manual knob adjustment? | In armed AI Only mode, respond promptly to ordinary manual knob adjustments with smooth, bounded recovery to the valid AI plan. No B2B hold or wait for the next transition. Global disarm, fresh-state checks and other execution guards still apply. | User |
| R2-Q05 | Where should quick Disarm AI controls be available? | Provide a persistent on-screen Disarm AI button, a keyboard shortcut and an assignable physical MIDI-controller button. All reach deterministic disarm handling without waiting for the planner, apply in every mode, and leave current music playing. Record their input source and outcome in session history. | User |
| R2-Q06 | When switching between AI Only and B2B during playback, should AI continue under the new mode or wait for another resume action? | When already armed, switching between AI Only and B2B continues automatically under the new mode after old-mode action cancellation and current-state reconciliation. Preserve explicit disarm: a mode switch cannot arm a disarmed service. Playlist Only still disables AI performance output; its return-to-performance behavior remains to be specified. | User |
| R2-Q07 | When returning from Playlist Only to AI Only or B2B while music is playing, how should AI performance begin? | Switching from Playlist Only to AI Only or B2B is an explicit request to begin AI performance after stale-action cancellation and current-state reconciliation; no extra Start/Resume is needed for the mode transition itself. Distinguish mode-based suspension from an explicit Disarm AI latch, which remains until deliberately resumed. Never replay historical or old-mode commands. | User |
| R2-Q08 | In B2B, if you manually load a different song in place of the AI queued song, what should happen to the original queued song? | Respect the human-selected replacement and adapt the mix and remaining set. Displaced-entry accounting is resolved by the delegated R2-Q10 default. | User |
| R2-Q09 | In B2B, if you press Pause or Stop on a deck, how should the AI respond? | Treat manual Pause/Stop as a latched transport hold on that deck. Cancel dependent transition actions; do not automatically play, cue-trigger or replace its track. Other independent work can continue. Human playback or explicit release of that deck hold permits fresh reconciliation; the ordinary 3-second timer cannot clear it. | Assistant default |
| R2-Q10 | What happens to the unplayed song displaced by a manual replacement? | Preserve the displaced occurrence in the remaining set, reorder around the human choice and record the replacement. Never reload it immediately over the chosen song. If it already played partly, preserve its actual spans and interrupted status; completion follows meaningful-playback rules. An explicit user skip resolves it without falsely marking it played. | Assistant default |
| R2-Q11 | How long can I reserve controls for myself in B2B? | Provide per-control and per-deck manual pins in addition to routine transition holds. Pins remain until explicitly released; global Disarm remains separate. Releasing a pin permits reconciliation and the next eligible transition, not an abrupt value reset. | Assistant default |
| R2-Q12 | How should AI infer interaction on a controller without touch sensors? | Use raw intent where available, including repeated identical values; otherwise use observed changes with uncertain origin. Show inferred inactivity rather than claiming physical touch detection. Apply the confirmed 3-second quiet and later-transition rules; uncertain manual divergence yields conservatively. | Assistant default |
| R2-Q13 | How should physical knobs catch up after AI changes their software values? | Preserve existing mapping behavior and verify pickup for each fixture. Offer supported soft takeover configuration with a clear mismatch indicator. Do not assume every controller supports pickup or motor feedback; do not reroute hardware through the AI service. | Assistant default |
| R2-Q14 | May AI create or change cue points? | Read user cue/hotcue/intro-outro data and preserve it. Store AI entry/exit suggestions in the app; explicit Save cues can write supported markers through a verified host operation with conflict handling. Triggering a hotcue is distinct from editing its stored position. | Assistant default |
| R2-Q15 | How should B2B handle manual jogging, scratching, seeking and loops? | Cancel stale track/position actions, yield the affected transport and dependent transition steps, and use fresh beat/playhead/loop state afterward. Touch release and later-transition eligibility still apply; no snapback to the abandoned position. Independent work may continue. | Assistant default |
| R2-Q16 | May I manually introduce a song outside a playlist-restricted set? | Music-pool restrictions constrain AI selection. A human load is respected in B2B and recorded with manual provenance, without silently enabling free AI selection or deleting original required occurrences. Recheck duration and pins after the change. | Assistant default |
| R2-Q17 | What should AI Only do after a manual track load? | AI remains authoritative in AI Only, but stale commands and active-deck replacement guards still apply. Observe the new identity/state, then choose a bounded recovery from the actual musical situation. Ordinary knobs recover promptly; manual loads never authorize blindly replaying old loads. | Assistant default |
| R2-Q18 | What should happen when AI planning is late or fails? | A planner timeout can fall back to constrained ordering and a conservative prepared transition if state, analysis, capabilities and authority are fresh. Clearly label fallback. Otherwise preserve current audio, stop starting new transitions and show action needed. Never invent a late plan or blindly loop/replay actions. | Assistant default |
| R2-Q19 | What should happen if MIDI feedback or the host connection is lost? | Loss of trustworthy host feedback disarms new performance output. Cancel queues, release AI-owned momentary controls through a verified watchdog path, and preserve current playback where possible. Reconnection requires negotiation, fresh snapshots and deliberate rearm. A physical-controller disconnect alone yields/cancels uncertain affected B2B work and alerts; do not automatically grab its held controls. | Assistant default |
| R2-Q20 | What should the initial mode and controller setup be? | First launch selects B2B disarmed; later launches remember the selected mode but remain disarmed. Use Reloop Buddy as a candidate, not a verified device. Setup must identify the actual attached model, mapping and ports; the unspecified Hercules model remains an evidence prerequisite, not a question blocking this plan. | Assistant default |

## Round 3 — Session UI/history, operational limits and acceptance

| ID | Question | Decision | Source |
| --- | --- | --- | --- |
| R3-Q01 | What should the main live session screen emphasize? | Show deck/track lanes, overlap, playhead, section boundaries, parameter curves and concise AI reasons. Label planned, sent, observed and uncertain states separately. Keep mode and Disarm visible. Raw bytes and causal/device details are available in an inspector. | Assistant default |
| R3-Q02 | When should MIDI session recording run? | Record raw AI output, physical input, host feedback, semantic events, intentions and outcomes by default whenever the app session is active. Continue across mode changes and disarm. Pausing capture creates an explicit gap; resume begins with a fresh snapshot. Include unknown/unmapped events, never mislabel them as AI or human. | Assistant default |
| R3-Q03 | Should sessions record audio as well as MIDI data? | Master-output audio capture is opt-in and separate from MIDI history. No microphone capture or broadcasting by default. The UI can attach an existing local recording and disclose alignment uncertainty. Record/broadcast capability exists in the coverage target but requires explicit operator intent. | Assistant default |
| R3-Q04 | How should track waveforms appear? | Generate or read verified local waveform assets outside MIDI handling; cache by file fingerprint. Show a timeline with duration/position while unavailable. Never present decorative or synthesized bars as measured audio. Audio playback in history, if enabled, remains isolated from performance MIDI. | Assistant default |
| R3-Q05 | How should long sessions be stored and rendered? | Use recoverable journal chunks and indexed snapshots; stream versioned JSON export. Propose 16 MiB chunks, snapshots at most 5 seconds apart, 1,000-event query pages and a separately bounded recorder queue. These are tunable design budgets, to be validated under dense jog/14-bit traffic. Raw capture is retained; only visual curves are downsampled. | Assistant default |
| R3-Q06 | What initial session size should we validate? | Initial acceptance envelope: 1 to 250 occurrences, roughly 10 minutes through 6 hours, including duplicates and open-format material. These are proposed test targets, not measured capacity or permanent product limits. Larger inputs must receive a visible capacity/preparation assessment without silent truncation. Full-feature inventory is not limited to two decks just because the first proof is. | Assistant default |
| R3-Q07 | How should history retention work? | Keep local history until deletion by default. Offer opt-in age/size cleanup, pinned sessions excluded. Show usage and low-disk warnings; propose warning at 5 GiB free and stop optional audio before data becomes endangered. Disk-full must preserve MIDI responsiveness and mark capture gaps, never silently erase sessions. | Assistant default |
| R3-Q08 | How should sharing and export handle local files? | Default local export includes event/schema/profile versions and stable track/occurrence references. Sharing mode removes unnecessary absolute paths and device identifiers; audio/waveform assets are opt-in with explicit relative references. Import validates sizes, versions and path traversal, remains read-only, and never sends MIDI. | Assistant default |
| R3-Q09 | How can I correct what AI learns? | Support optional timestamped notes and transition/set ratings, inspectable learned preferences with evidence, edit/delete/reset and a disable-learning switch. Never treat every B2B gesture as a permanent preference. Learning is local and contextual; user-specified settings and pins win. | Assistant default |
| R3-Q10 | How far back should repetition and preference learning look? | Default repetition checks prioritize the current session, with recent local history (30 days, configurable/off) as a soft signal. Prefer unplayed sections where musically useful; do not ban repeated material. Display/reset evidence and keep event archives separate from the preference model. | Assistant default |
| R3-Q11 | What should matching lyrics mean musically? | Use confident repeated hooks, words or phrases for potential handoffs; themes and language are weaker optional selection signals. Respect phrase timing, BPM, harmony and vocal density. Never force a lyrical match that creates clashes; allow user emphasis, language and disable controls. | Assistant default |
| R3-Q12 | Where should lyrical information come from initially? | Use available local tags/sidecar timed lyrics and optional local transcription in a separate analysis process. Preserve source, confidence and timing; no mandatory cloud service or automatic online lyric collection. Missing lyrics reduces that score, never makes a track unplayable. Select/benchmark actual tools during implementation, not by invented capability. | Assistant default |
| R3-Q13 | What initial tempo-change limits should AI use? | Propose absolute tempo offsets within 3% as preferred and 6% as the normal cap, configurable per set/track and distinguishable from half/double-time analysis. Use a different transition when limits conflict; evaluate pitch/key-lock effects by listening. Numeric values are assistant defaults, not user-specified musical laws. | Assistant default |
| R3-Q14 | Which defaults should apply to a newly created set? | Default to supplied-playlist pool, source snapshot until refresh, review-first start and local autosave with optional new Mixxx playlist. Retain all requested alternatives: free library selection, live source sync, automatic start for an explicitly initiated set, automatic dual save or local-only/manual export. Keep these independent of performance mode. | Assistant default |
| R3-Q15 | How much musical planning can I override? | Expose AI-selected defaults and user editing for style, energy presets/custom curve, opener/closer and next-track pins, duration/range, tempo limits, section/cue boundaries and transition intensity. Changes validate feasibility and invalidate only affected future plans. Preserve active transitions and flag impossible duration/all-song combinations. | Assistant default |
| R3-Q16 | How should the local desktop app launch? | Deliver a local Mac launcher and TypeScript/HTML browser UI with a separate persistent Node service. Closing the tab does not kill performance. Bind to loopback, validate origin/session credentials and avoid exposing control APIs on the LAN. Desktop packaging must be tested on a clean local setup; an Electron wrapper is not required for this build. | Assistant default |
| R3-Q17 | What responsiveness targets should we test first? | Propose command-send to host-observation p95 <=30 ms and p99 <=75 ms; recognized human-input to AI cancellation p95 <=20 ms, p99 <=50 ms; observed state to app UI p95 <=100 ms. Report native Mixxx UI and audible onset separately, plus clock uncertainty, worst case and misses. No hard-real-time guarantee. Freeze a musically relevant host-quantized scheduling gate and measured onset budget in F01/M19 before claiming beat-critical support; failing budgets trigger focused optimization/native-scheduler work. | Assistant default |
| R3-Q18 | What should validate musical quality? | Use repeatable Mixxx AutoDJ baseline and level-matched blind excerpts where feasible, covering open format, tempo/key differences, duplicate sections and manual interventions. Propose human ratings >=4/5 for naturalness and enjoyment on representative accepted sets, with no severe unintentional clashes, gaps or clipping. Keep subjective results and technical failures visible; human feedback is required before a runtime release. | Assistant default |
| R3-Q19 | What happens when analysis, recording or UI is overloaded? | Prioritize cancellation/releases and host state over telemetry, analysis, export and rendering. Bounded queues, process isolation and backpressure apply. Pause optional analysis/audio capture and downsample visual rendering before losing control responsiveness. Record gaps honestly; stale essential feedback disarms. Never drop already captured raw events silently while reporting lossless history. | Assistant default |
| R3-Q20 | What should count as completion of this phase? | Complete discovery with explicitly delegated defaults, aligned goals/specs, an updated HTML direction page, bounded worker cards and traceability/validation, then commit/push planning artifacts to the existing fork. Runtime implementation starts only on a later user instruction. Unverified device/tool/performance facts remain future evidence requirements, not silently answered facts. | Assistant default |

## Evidence still needed during implementation

Hardware identity and mapping, exact runtime version, native library access, waveform/lyric providers, capacity, timing and listening quality must be verified. Delegation settles planning choices; it does not manufacture those facts. See [worker tasks](WORKER-TASKS.md) and [planning defaults](DEFAULTS.md).

> End of autonomously AI-generated planning record.
