# 02-inventory

> Autonomously AI-generated planning cards. No implementation claimed.

[Worker rules](../WORKER-TASKS.md) · [Catalog](README.md)

## I01: Inventory deck transport — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/transport/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Deck transport; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I01.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I02: Inventory mixer — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/mixer/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Mixer; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I02.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I03: Inventory tempo and sync — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/tempo-sync/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Tempo and sync; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I03.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I04: Inventory eq and filter — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/eq-filter/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for EQ and filter; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I04.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I05: Inventory effects — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/effects/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Effects; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I05.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I06: Inventory cues and loops — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/cues-loops/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Cues and loops; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I06.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I07: Inventory jog and scratch — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/jog-scratch/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Jog and scratch; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I07.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I08: Inventory samplers and preview — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/samplers-preview/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Samplers and preview; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I08.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I09: Inventory stem decks — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/stems/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Stem decks; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I09.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I10: Inventory microphone auxiliary and vinyl — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/inputs-vinyl/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Microphone auxiliary and vinyl; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I10.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I11: Inventory library navigation — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/library-navigation/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Library navigation; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I11.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I12: Inventory load selected track — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/selected-load/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Load selected track; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I12.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I13: Inventory stable track selection and playlists — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/catalog-selection/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Stable track selection and playlists; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I13.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I14: Inventory track metadata — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/metadata/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Track metadata; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I14.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I15: Inventory beat phrase and energy analysis — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/analysis/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Beat phrase and energy analysis; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I15.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I16: Inventory mixxx autodj — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/autodj/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Mixxx AutoDJ; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I16.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I17: Inventory recording — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/recording/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Recording; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I17.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I18: Inventory broadcasting — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/broadcast/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Broadcasting; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I18.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I19: Inventory skin waveform and display — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/display/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Skin waveform and display; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I19.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I20: Inventory sampler bank files — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/sampler-banks/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Sampler bank files; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I20.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I21: Inventory library management — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/library-management/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Library management; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I21.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I22: Inventory preferences and setup — first page

**Worker:** L1 / `gpt-5.6-luna` / medium. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** F10, F01, F11. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/preferences/page-01.json`

**Scope:** At most 20 canonical candidates or one menu subtree; create INV-PAGE continuations for the rest.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Inspect the chosen profile source/manual entries for Preferences and setup; capture canonical IDs, aliases, units, read/write/trigger semantics and dynamic instance rules.
2. Cross-check the relevant native UI/menu using its exclusive fixture lease; record missing controls, hidden operations, version differences and the next cursor.

**Validate:**

1. Check each row has source/profile and separate command, observation, UI and runtime status; none starts verified.
2. Compare covered subtree and pagination against full family; explicitly list remaining scopes and hardware requirements.

**Evidence:** `docs/ai-dj/work/evidence/I22.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## I23: Close the full initial inventory

**Worker:** L3 / `gpt-5.6-sol` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** I01, I02, I03, I04, I05, I06, I07, I08, I09, I10, I11, I12, I13, I14, I15, I16, I17, I18, I19, I20, I21, I22. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `docs/ai-dj/work/inventory/index.json`, `docs/ai-dj/work/inventory/closure.md`

**Scope:** Merge up to 40 new rows per pass using INV-MERGE; final closure only when all pages done.

**Prerequisites:** All family pages and any INV-PAGE/INV-MERGE children accepted; selected runtime profile available.

**Evidence gates:** Inventory continuation manifest closed; every family and page accounted for.

**Perform:**

1. Merge family manifests, deduplicate aliases, and expand configured instances without treating absent controls as zero.
2. Review generated declarations, manual and UI/menu scope checklists; keep all continuation IDs in closure manifest.

**Validate:**

1. Require all 22 families and every declared page/subtree resolved or explicitly unknown.
2. Unknown inventory scope prevents closure; capability implementation gaps are recorded and do not imply missing inventory.

**Evidence:** `docs/ai-dj/work/evidence/I23.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

> End of autonomously AI-generated planning cards.
