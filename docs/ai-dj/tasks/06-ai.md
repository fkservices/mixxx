# 06-ai

> Autonomously AI-generated planning cards. Task specifications with recorded execution status; completion requires acceptance evidence.

[Worker rules](../WORKER-TASKS.md) · [Catalog](README.md)

## A01: Define local analysis feature contract

**Worker:** L4 / `gpt-6-astra` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** D01, P08. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `ai-dj/core/analysis.ts`, `docs/ai-dj/work/contracts/analysis.md`

**Scope:** Tempo/key/energy/phrase/intro-outro/vocal-risk data contract.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Record source fingerprint, analysis version, units, confidence and manual overrides per feature.
2. Define missing/low-confidence behavior and separate audio analysis data from MIDI telemetry.

**Validate:**

1. Check stale analysis after file replacement is rejected.
2. A track without phrase/vocal confidence still has a conservative eligible template.

**Evidence:** `docs/ai-dj/work/evidence/A01.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A02: Choose bounded local analysis tools

**Worker:** L3 / `gpt-5.6-sol` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A01. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `docs/ai-dj/work/analysis-choice.md`

**Scope:** Evaluate at most two candidates against local fixtures; discovery only.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Check official current docs/licensing and available local runtime for required features.
2. Select candidate and enumerate per-feature adapter tasks, compute limits and preparation stage.

**Validate:**

1. List measured versus untested claims and required fixture/reference data.
2. No claim MIDI exposes audio, beat grids or vocal stems automatically.

**Evidence:** `docs/ai-dj/work/evidence/A02.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A03: Implement analysis cache and job boundary

**Worker:** L2 / `gpt-5.6-terra` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A02, P09. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `ai-dj/analysis/cache.ts`, `ai-dj/analysis/jobs.ts`, `ai-dj/test/analysis/cache.test.ts`

**Scope:** Cache and process contract, not all DSP adapters.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Use fingerprint+version cache keys and bounded local worker messages.
2. Run preparation outside communicator and persist partial progress.

**Validate:**

1. Test stale fingerprint, canceled job and invalid result.
2. Analysis cannot block MIDI or treat partial output as complete.

**Evidence:** `docs/ai-dj/work/evidence/A03.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A04: Specify one adapter task per analysis feature

**Worker:** L3 / `gpt-5.6-sol` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A03. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `docs/ai-dj/work/analysis-expansion.json`

**Scope:** Create at most five bounded ANALYSIS children from A02.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Allocate bounded ANALYSIS children for verified missing tempo/key, energy/loudness, phrase/section, vocal/lyric and waveform capabilities as needed.
2. Each child names exact provider/function, source/version, fixture, confidence and fallback; create a continuation for more than five children.

**Validate:**

1. Cover each A01 field with existing verified host data or an implementation child.
2. Missing detector capability becomes explicit uncertainty plus fallback, not invented measurement.

**Evidence:** `docs/ai-dj/work/evidence/A04.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A05: Verify prepared analysis on fixture tracks

**Worker:** L3 / `gpt-5.6-sol` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A04. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `docs/ai-dj/work/runs/A05.json`

**Scope:** At most five fixture tracks in one review batch.

**Prerequisites:** All required ANALYSIS child results accepted; large analysis jobs complete via persistent job workflow.

**Evidence gates:** Required ANALYSIS children and terminal analysis-job reviews accepted.

**Perform:**

1. After ANALYSIS children and long-job reviews, compare cached results to prepared reference markers.
2. Record errors and confidence for steady beat, variable tempo and misleading/doubled BPM.

**Validate:**

1. Require source/version provenance and manual override precedence.
2. Keep unsupported/low-confidence features marked; do not infer enjoyment from scores.

**Evidence:** `docs/ai-dj/work/evidence/A05.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A06: Implement constrained next-track scoring

**Worker:** L2 / `gpt-5.6-terra` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A01, P11. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `ai-dj/planner/scoring.ts`, `ai-dj/test/planner/scoring.test.ts`

**Scope:** Deterministic scorer over remaining occurrences.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Score tempo/key/energy trajectory and repetition spacing with hard pins and capability constraints.
2. Use deterministic tie-breaking and confidence-aware penalties.

**Validate:**

1. Test every occurrence preserved, no invented track identity and pins respected.
2. Bad confidence selects conservative compatibility rather than discarding songs.

**Evidence:** `docs/ai-dj/work/evidence/A06.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A07: Define and validate AI structured output

**Worker:** L4 / `gpt-6-astra` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A06, F08, D01. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `ai-dj/planner/contract.ts`, `ai-dj/planner/validate.ts`, `ai-dj/test/planner/validate.test.ts`

**Scope:** Plan schema and runtime validation.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Require occurrence order, transition template/length, bounded curves, confidence/reasons and mode/track/set epochs.
2. Reject unsupported actions, stale plans, unbounded durations and arbitrary function text.

**Validate:**

1. Reject malformed, stale or disallowed actions and invalid omission/addition accounting for the chosen music pool.
2. Playlist Only cannot output performance commands; invalid plans never reach MIDI.

**Evidence:** `docs/ai-dj/work/evidence/A07.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A08: Choose local model and inference runtime candidate

**Worker:** L4 / `gpt-6-astra` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A07, F01. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `docs/ai-dj/work/model-choice.md`

**Scope:** Compare at most two candidates; no large download wait.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Check current official requirements and local resource fit for structured planning.
2. Choose one candidate, bounded prompt/response and timeout; launch install/download as JOB if needed.

**Validate:**

1. Record actual availability, license/version and memory estimates as unmeasured until benchmark.
2. Preserve all-local operation and no cloud dependency.

**Evidence:** `docs/ai-dj/work/evidence/A08.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A09: Implement isolated planner process adapter

**Worker:** L3 / `gpt-5.6-sol` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A08, A07. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `ai-dj/planner/process.ts`, `ai-dj/test/planner/process.test.ts`

**Scope:** One inference runtime interface.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Send bounded inputs asynchronously and parse validated replies; enforce deadline/cancel.
2. Kill/restart stalled planner separately from MIDI process with no automatic action replay.

**Validate:**

1. Test process crash, malformed reply and response after mode switch.
2. Pending inference cannot block direct disarm or feedback handling.

**Evidence:** `docs/ai-dj/work/evidence/A09.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A10: Implement complete playlist ordering policy

**Worker:** L3 / `gpt-5.6-sol` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A09, A06, P10, P19, P21, A15, A16. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `ai-dj/planner/order.ts`, `ai-dj/test/planner/order.test.ts`

**Scope:** Bounded order/replan algorithm for a configured maximum playlist size.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Combine AI proposal with hard occurrence/pin constraints and deterministic fallback.
2. Replan only uncommitted remainder after edits/manual actions; preserve meaningful selected-section policy.

**Validate:**

1. Verify multiset coverage, duplicates, pins, impossible duration and edited set revision.
2. Long lists use chunked/bounded planning with visible limits, never silent truncation.

**Evidence:** `docs/ai-dj/work/evidence/A10.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A11: Connect transition choices to live set runner

**Worker:** L3 / `gpt-5.6-sol` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A10, D05, A05, P20, P22, P23, A18. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `ai-dj/planner/conductor.ts`, `ai-dj/test/integration/conductor.test.ts`

**Scope:** Plan-ahead integration only.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Prepare the next bounded transition before its deadline; executor retains timing authority.
2. In B2B pass holds/latest values; in AI Only plan valid recovery; in Playlist Only return arrangement only.

**Validate:**

1. Test late AI, user bass, manual load and playlist-only run.
2. Selected model never emits time-critical CCs or overrides disarm.

**Evidence:** `docs/ai-dj/work/evidence/A11.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A12: Benchmark planner load and communicator isolation

**Worker:** L3 / `gpt-5.6-sol` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A11, M19. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/runs/A12.json`

**Scope:** At most eight minutes with actual chosen local model.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Chosen model is actually installed; download/install JOB results accepted.

**Perform:**

1. Repeat MIDI timing sequence under real inference load; capture CPU/memory/audio overload and missed deadlines.
2. Stall planner and close UI while communicator keeps running.

**Validate:**

1. Compare against idle and synthetic-load runs using same timing definitions.
2. Require responsive feedback/release/disarm; failures create focused scheduler or resource tasks.

**Evidence:** `docs/ai-dj/work/evidence/A12.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A13: Record AutoDJ comparison baseline

**Worker:** L3 / `gpt-5.6-sol` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** D07. **Leases:** mixxx-runtime. **Status:** planned.

**Owns:** `docs/ai-dj/work/runs/A13.json`

**Scope:** One short fixed playlist baseline; long sets use JOB workflow.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Accepted dependency evidence and dispatch checks.

**Perform:**

1. Record same fixtures using chosen Mixxx AutoDJ configuration with AI executor disarmed.
2. Save order, settings, levels and audio for matched comparison.

**Validate:**

1. Confirm only one executor owns transition at a time.
2. Check recordings are comparable in source/level/duration and preserve original files.

**Evidence:** `docs/ai-dj/work/evidence/A13.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

## A14: Review AI musical quality and constraints

**Worker:** L4 / `gpt-6-astra` / high. **Budget:** 25 minutes; maximum 30 including handoff.

**Dependencies:** A12, A13. **Leases:** Exact path reservation. **Status:** planned.

**Owns:** `docs/ai-dj/work/reviews/ai-quality.md`

**Scope:** One bounded listening packet; human review separate.

**Prerequisites:** Dependencies accepted on the integrated revision; common dispatch prerequisites in WORKER-TASKS.md apply.

**Evidence gates:** Before marking this review accepted: Human comparison ratings available; otherwise prepare packet then leave acceptance pending.

**Perform:**

1. Prepare matched AI/baseline excerpts and rubric for phrasing, level continuity, vocal clash, pacing and all-song coverage.
2. Collect human ratings/annotations and create small defect follow-ups.

**Validate:**

1. No unit test or model self-rating certifies enjoyable output.
2. Require full recorded-set review later; missing listener response stays pending.

**Evidence:** `docs/ai-dj/work/evidence/A14.md`. Stop expanding scope by minute 20. If any remaining execution or validation cannot finish by minute 25, create bounded continuation cards using SPLIT; hand off by minute 27. Supervisor stops worker at minute 30.

**Completion:** All required children/continuations, long-job results and affected integration checks must be accepted; conditional children require trigger evidence or explicit not-triggered finding.

> End of autonomously AI-generated planning cards.
