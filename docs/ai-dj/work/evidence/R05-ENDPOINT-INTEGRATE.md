# Mapping endpoint integration acceptance

> Autonomously AI-generated acceptance evidence.

Eight fragments assemble reproducibly into the mapping. The verified native
SysEx callback shape, explicit clock/handler configuration and generation-bound
wrapper coexist with the conventional modules. Configuration rejects missing
validators, duplicate roles and implicit diagnostic access. Unregistered or
invalid payloads cannot dispatch a handler. Semantic authority remains separate.

Assembly regressions cover conventional routing/feedback, opt-in activation,
failed initialization cleanup, diagnostic restrictions, clock regression, reset,
input loss, queued output closure and stale callbacks. Timer cleanup failure
now prevents reinitialization even when an earlier endpoint fault masks it.
All 178 software tests, typecheck/build and deterministic mapping checks passed.
Native clock, reset/input-loss and queued reply probes verify actual host APIs.

[Integration notes](../integrations/wire-endpoint.md),
[native reply lifecycle](../runs/R05-REPLY-LIFECYCLE.json) and
[native timer record](../runs/R05-NATIVE-SEND-TIMER.json) link the evidence.
This accepts mapping integration, not the full R05 endpoint gate. The separate
R05-ENDPOINT-NATIVE card still requires its consolidated diagnostic/handoff
review. No production semantic handlers or DJ authority are enabled by default.

> End of autonomously AI-generated acceptance evidence.
