# INTEGRATE-F09-SYNC-CONTRACT evidence

> This document was autonomously generated by an AI agent at the user's request.

## Result and ownership

Prepared for coordinator review against input `0eeae4447ca1f6a1f2e3af683ee8061a85888481`, with F06/F07/F08/F09 accepted. Owned changes are only `ai-dj/core/actions.ts`, `ai-dj/core/capabilities.ts`, `docs/ai-dj/work/contracts/actions.md`, `docs/ai-dj/work/contracts/state.md` and this evidence. No validator, profile, ledger, runtime/device configuration or peer file was edited. No commit, host launch, port operation, self-acceptance or subagent was performed.

The closed version-1 `LegacySemanticAction` remains unchanged. New `SyncSemanticAction` uses action schema 2 solely for `deck.set_sync_enabled`, desired-state boolean `args.enabled`, one closed sync-context guard and participant-aware composite authority. The capability selector requires schema version 2 explicitly. Outcome event variants retain their meanings; version-2 sync outcomes must resolve to a version-2 action and legacy events remain version 1. The current validator intentionally rejects the new envelope, and the conventional profile remains extension-needed.

The guard references a complete configured roster of 1–16 participants, not just visible/mapped decks. The F11 fixture's two visible decks versus persisted four-deck configuration is an explicit counterexample to treating skin visibility as closure. Exact roster/guard/deck-precondition sets agree; target must be loaded. A known-empty peer is represented without fake BPM values and requires explicit ineligible/nonparticipating evidence. There is one composite deck-sync grant per roster deck plus a shared engine grant, at most 17 claims. Composite grants overlap existing transport/track/cue/jog and sync-related controls, preserve manual holds and existing cue cleanup, and cannot be minted by action data.

Named canonical telemetry and exact types/bounds cover sync mode/enabled, playing/audibility, passthrough/vinyl/quantize, loaded eligibility/grid/BPM/rate/phase, shared leader/internal clock and pending sync changes. All observation IDs are distinct and resolve to exact current feature/instance/binding evidence. `SyncTopologyEvidence` and `SyncGuardAvailability` describe bounded immutable reference data, not callable code or implemented discovery. The `[0,3]` tempo-change admission bound allows up to fourfold increase and is distinct from the product's configurable 3% preferred / 6% normal policy defaults. Effect limits must still resolve to approved current policy; phase/tempo safety is never inferred merely from the request.

## Source basis and graph limits

Read GOALS.md, the current expansion card, full owned source/contracts, relevant F07 state source, F09 conventional contract and its accepted evidence. Parent Verify evidence: `mixxx-ai-dj`, generation `2026-09-13T16:44:03Z`, ready, 47,976 nodes / 238,220 edges; relevant new-symbol search zero/no-more and action/capability/profile paths not tracked/coverage unavailable. Direct source reads supplied this unindexed contract review. No graph completeness claim is made.

No new released-host behavior is inferred from the newer local checkout. Re-read the F09 downloaded **official 2.5.6** sources and verified their SHA-256 values against accepted F09 evidence:

| Source | Verified SHA-256 | Contract consequence |
| --- | --- | --- |
| [synccontrol.cpp](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/engine/sync/synccontrol.cpp#L144) | `89fb09a465dc8b721dc4ecf7ce0ba8ed2af642cc0ab6bf24832ef91bed2b9ca7` | Confirmed mode/enabled differs from request; follower activation can disable vinyl, passthrough matters, playing plus quantize can request phase alignment |
| [enginebuffer.cpp](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/engine/enginebuffer.cpp#L413) | `ab066eb911dab91f33d4f1ff87d6c31aacc2c3da413638c9e0184c2f9be9f256` | Rapid enable/disable is an explicit queued one-shot case; cancellation cannot be implemented as a blind disable |
| [enginesync.cpp](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/engine/sync/enginesync.cpp#L33) | `3a105d558fe2030f285734588843e73865716d10cf520bba716ca82baccd142e` | Follower request can select a soft leader and update other participating decks; target-only ownership is insufficient |

No selected leader, exact BPM, phase deadline or MIDI-clock synchronization is promised. Already-satisfied state requires fresh evidence and no redundant write. A rejected enable uses actual unchanged-state evidence; missing feedback stays uncertain. No pulse, automatic release, blind retry or compensating write is authorized. Conventional feedback has no request IDs, causal acknowledgement, deduplication or actor identity. Boolean satisfaction is distinct from tempo/phase/participant effects and musical success.

## Checks

Runtime: Node `v26.8.1`, npm `11.19.0`. Commands ran from repository root on 2026-09-13:

| Check | Result |
| --- | --- |
| `npm --prefix ai-dj run typecheck` | Exit 0 |
| `npm --prefix ai-dj run build` | Exit 0 |
| `npm --prefix ai-dj test` | Exit 0; 50 tests passed, 0 failed, including all 28 accepted validator regressions and concurrent MIDI work |
| `npm --prefix ai-dj test -- test/core/validate.test.ts` | Exit 0; 28 tests passed, 0 failed |
| `git diff --check` | Exit 0, no output |
| Temporary compile fixture with strict NodeNext TypeScript settings | Exit 0; positive true/false sync operations and versioned selector; `@ts-expect-error` checks reject sync in version 1, unversioned selector, toggle kind and missing guard tuple |
| Temporary capacity/gating fixture | Exit 0; outputs below |

Temporary review checks are `/tmp/ai-dj-sync-contract-check.ts` and `/tmp/ai-dj-sync-capacity-check.mjs`, outside repository ownership. Compile command: `./ai-dj/node_modules/.bin/tsc --noEmit --strict --module NodeNext --moduleResolution NodeNext --target ES2024 --allowImportingTsExtensions --types node --typeRoots ./ai-dj/node_modules/@types /tmp/ai-dj-sync-contract-check.ts`. Capacity command: `node /tmp/ai-dj-sync-capacity-check.mjs`. These are disposable checks, not committed runtime tests; the next validator child owns lasting admission tests.

```text
PASS v2 remains gated: invalid-literal
PASS capacity fixture: 16 loaded participants, 17 grants, 128-byte IDs, 61670 bytes, 1054 JSON nodes
BOUNDARY: synthetic type/size checks only; runtime guards unavailable until implemented
```

The capacity fixture expands all IDs to 128 bytes and counts complete JSON data nodes; large timestamps/counters are for serialized-size measurement, not a claim that the fixture's timing is an executable action. It fits current 65,536-byte / 4,096-node transport limits without increasing either. Cardinality ceilings do not waive byte limits. One initial temporary script used the wrong diagnostic property (`issues` instead of `errors`) and failed before capacity checks; corrected script produced the output above. No source defect or test failure was concealed.

## Required next inputs and remaining gates

1. **INTEGRATE-F09-SYNC-VALIDATION**, already materialized: own only `ai-dj/core/validate.ts`, `ai-dj/test/core/validate.test.ts` and its evidence. Inputs are these accepted types and both normative contracts, F08 action/state evidence and F09. Add explicit schema dispatch: unchanged version-1 actions/outcomes, version-2 sync only, and version-matching outcome shapes. Validate both booleans, exactly one sync-context condition, roster/deck/target/grant set equality, 1/16/17-participant boundaries, loaded/empty discrimination, unique observation IDs, sync mode/leader consistency, numeric/policy limits, malformed/extra fields and unknown unrelated actions. Preserve immutability and all F06/F07 regressions. Well-shaped guard references do not prove their truth or grant authority. Run focused validator tests, typecheck/build and the full suite; review independently.
2. **INTEGRATE-F09-SYNC-PROFILE**, already materialized after validation: own its declared profile/spec/evidence only. Bind schema-2 selector without changing F09 bytes or desired-state meaning. This specifies encoding only. Preserve `acknowledgement:none`, `deduplication:none`, unknown attribution and raw feedback boundaries. **Production semantic sync remains unavailable until independently verified topology/guard/queue/race/effect telemetry and host guarding exist.** A specified selector or raw M08 mapping proof cannot close this gap.
3. **Required host-operation work for later OP expansion:** resolve complete configured sync topology and instance eligibility; expose trustworthy named guard/leader/queue evidence; specify bounded host-side precondition checking/race handling and tempo/phase effect verification; integrate their source evidence and authority overlap rules. Suggested first bounded specification target is `mixxx.sync-guard.v1`, covering these exact fields and fail-closed unavailable results, before implementation is split into the relevant host/reconciler/authority owners. This worker does not materialize or accept those cards. Missing telemetry is an explicit supported-data gap, not a hidden assumption or permission to infer safety from MIDI feedback.

M03 still requires accepted validator and profile integrations before semantic encoding of sync. M08 may prove the raw released setter in its authorized fixture but cannot claim guarded production sync. Real host/native-UI/audio, timing and human review remain separate gates. No continuation is needed for this bounded contract child; coordinator review/acceptance and the required downstream work remain outstanding.

> End of autonomously AI-generated evidence.
