# M13 native mouse feedback investigation

> Autonomously AI-generated interim evidence. M13 remains incomplete.

The installed Mixxx 2.5.6 LateNight fixture host and disarmed TypeScript service
were inspected on 2026-09-13. [Structured observations](../runs/M13.json)
include hashes of private screenshots, status snapshots and a frozen capture.

Mouse actions changed deck 1 volume from 1 to 42/127 and crossfader from 0.5
to approximately 0.754. Mouse play and pause changed returned play state.
The frozen interval contains 45,596 incoming raw feedback events and zero
outgoing commands. Capture drops remained zero. This proves observed feedback
without AI writes; it does not establish actor attribution or latency bounds.

A short native SYNC click briefly emitted `B8 22 7F`, then returned to
`B8 22 00`. Native tempo changed from 117 to 102 BPM despite the final sync
value being false. This agrees with the documented distinction between a tap
and holding the button for sync lock in the
[Mixxx manual](https://manual.mixxx.org/2.5/en/chapters/djing_with_mixxx.html).
Final false therefore does not mean no sync action occurred.

The installed skin binds native CUE to `cue_default` and its indicator to
`cue_indicator`; the initial MIDI route observes `cue_preview`. A mouse click
created a visible cue marker at displayed 27.02 seconds. A subsequent click
returned to a paused deck. Both decks' raw cue values remained zero throughout
the frozen interval. The existing cue field cannot be presented as a complete
record of native CUE actions. The brief click does not prove sustained preview.

Before handoff, native mouse operations restored deck 1 tempo to 117 BPM and
0 percent rate, both volumes to 1 and crossfader to 0.5. Both decks were paused,
both sync readings false and the service disarmed. The newly created cue marker
remains in the isolated fixture profile; the test did not restore that marker.

## Remaining evidence

- Sustained cue preview captured in native UI, alongside returned state.
- Manual cue visibility with semantics that distinguish cue placement,
  indicator state and actual preview.
- Complete five-control agreement before accepting M13. Production cue/sync
  guards remain unchanged; mouse-only observations do not bypass them.

## Exclusive raw fixture follow-up

The original service PID 4355 was identity-checked and stopped with SIGTERM.
Its capture ended with `service-closed` and the process disappeared. A private
five-minute fixture runner then exclusively owned the same AI DJ virtual ports.
It accepted only fixed cue/sync experiments, required a fresh paused deck before
each command and released a cue after four seconds or on orderly exit. It is
not a production authority mechanism and was not added to the service API.

Toggling the mapping did not recover feedback immediately after replacing the
port owner. The isolated host was restarted, and its mapping was found disabled.
Enabling it restored feedback. The evidence does not isolate whether stale
endpoint binding or unsuccessful UI toggling caused the earlier loss.
Both approved fixtures were loaded again through native dialogs.

The resulting private capture contains 21,846 feedback packets and exactly seven
returned sends: deck 1 cue press/release, deck 2 cue press/release, deck 1 sync on,
deck 2 sync on and deck 2 sync off. Both cue experiments returned play and
cue-preview values true during the four-second interval, then false after
release. The native screenshots show the post-release paused positions; they
do not prove the held preview visually. No cue remained pending on runner exit.

Native screenshots show deck 1 sustained SYNC lit at 102 BPM, with its tempo
changed from 117 BPM. A mouse click extinguished it and returned sync false,
without another MIDI command. Deck 2 SYNC subsequently lit after its MIDI on
command and turned off after an explicit MIDI off command more than nine seconds
later. Feedback matched each state change. The runner did not echo feedback or
automatically reassert the manual override. This is a fixture observation, not
implemented B2B arbitration or a measured latency pass.

Finally, deck 1 tempo was reset through the native slider to 117 BPM, rate zero.
Both decks remained paused, both sync states false, volumes 1 and crossfader
centered. The runner closed with exit code zero before its hard lifetime.
Mixxx PID 34918 remains open with the isolated profile; the communicator is
currently stopped and must be deliberately restored for subsequent tests.

> End of autonomously AI-generated interim evidence.
