# F08 action-validation child evidence

> This document was autonomously generated by an AI agent at the user's request.

This bounded child completes the F06 action-side validator in [validate.ts](../../../../ai-dj/core/validate.ts) and its paired tests. It validates every closed semantic action: deck play, deck parameter, mixer parameter, cue goto, load, cue press, cue release, and relative jog. It enforces exact fields, IDs, safe generations including `trackGeneration` and `pressGeneration`, time horizons, required preconditions, ownership, nonconflicting/non-dangling host conditions, value bounds, and exact press/release lease pairing. Cleanup release requires its target loaded deck precondition and the same obligation/action/generation as cleanup authority.

It also validates every F06 action-outcome discriminant and both momentary-obligation evidence discriminants. Accepted values are deep-frozen copies. Bounded parsing rejects accessors before reading them, functions/non-JSON values, recursion, excessive depth/nodes/bytes, arbitrary fields, and unsupported action names. It contains no I/O, state reducer, action execution, MIDI, native host, timer, or script invocation.

Validation on the shared integrated tree: `npm run typecheck`, `npm test -- test/core/validate.test.ts` (9 tests), and `npm run build` all exited 0. The suite covers positive validation for every action, outcome, and obligation variant plus unsupported action, malicious field/accessor, malformed ID, absent generations, nonfinite/invalid range/time, contradictory conditions, lease mismatch, oversize, recursion/depth, and immutable-copy behavior.

F09's `deck.set_sync_enabled` remains rejected as an unknown operation because F06's frozen union does not define it. Its planned explicit contract/validator extension is required before it can pass this boundary.

This is not F08 acceptance. The F07 state-validator child remains necessary for pure immutable baseline projection, old-context stale evidence, positive deck current observations, profile/binding transaction checks, exact dynamic-instance/presence resolution, and adversarial state fixtures. It must replace the unaccepted shape-only state admission before downstream use.

## Coordinator acceptance checks

Coordinator review added pre-parse string nesting limits, bounded object keys and all-value node accounting, hidden/symbol/accessor rejection before serialization, strict ID endings, and the valid empty-deck parameter case. A stored cue obligation requires a loaded binding. The exact 65,536-byte accepted transport boundary and its one-byte rejection are tested. Integrated typecheck/build and all 18 tests passed after these corrections; 11 are in the validator suite. F07 state tests still cover only the partial implementation and do not close its separate child.

> End of autonomously AI-generated document.
