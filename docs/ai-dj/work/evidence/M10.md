# M10: assembled mapping and native load

> Autonomously AI-generated implementation evidence at the user's request.

Input revision `4b7c06c`; M06–M09 accepted. Coordinator execution under mapping-entry and mixxx-runtime leases. Existing keeper PID 73352 and isolated Mixxx PID 73988 were reverified; neither was restarted.

[Assembler](../../../../ai-dj/tools/build-mapping.ts) combines the four trusted source fragments in fixed order between unique markers in [the mapping](../../../../res/controllers/AI-DJ-scripts.js). It preserves the bootstrap outside those markers, rejects missing/reversed/duplicate markers, and provides `--check` for stale output. No timestamp, absolute path or machine-specific data enters the bundle. The XML description now reflects the installed initial controls; command addresses are unchanged.

The harness can isolate bootstrap/individual fragments or load the actual assembled bundle. M10 ownership includes this helper, [assembly tests](../../../../ai-dj/test/host/assembly.test.ts) and XML description integration. Tests verify reproducibility, boundary preservation, all four module initializations, 18 subscriptions, one refresh timer, initial packets, all five command families, separate feedback routes, cue shutdown cleanup and leak-free reinitialization.

## Checks

From the repository root:

| Check | Result |
| --- | --- |
| `node ai-dj/tools/build-mapping.ts` | Generated fixed-order bundle |
| `node ai-dj/tools/build-mapping.ts --check` | Output matches all four sources |
| `npm --prefix ai-dj test` | 92 passed |
| `npm --prefix ai-dj run typecheck` | Passed |
| `npm --prefix ai-dj run build` | Passed |

Commands used the local Node 26 toolchain. The assembler CLI is run from its TypeScript source path. Graph coverage for new/changed files was unavailable in generation `2026-09-13T16:44:03Z`; exact source and accepted fragment review supplied evidence.

## Native evidence

The final XML/script were copied only into the existing isolated Mixxx profile. Mixxx reloaded the script and logged `initialized; fragments=4` and 18 successful connection registrations. The first 18 output packets cover all nine presence/value pairs on the expected B8/B9/BA routes. There were no script exceptions, unavailable-route messages or failed cleanup in this initialization segment. Installed files matched repository SHA-256 hashes in [the captured runtime report](M10-runtime.json).

The existing Node endpoint recorded sampled packets with no transport loss. Its diagnostic capture is capped at 16 total packets, including the earlier probe; this proves reception of a subset, not all output routes or ongoing losslessness. The host console proves all initial output routes separately. This keeper's synchronous result-file updates and debug logging are unsuitable as final timing evidence; later measurement needs the planned capture tooling.

[Actual native screenshot](../screenshots/mixxx-assembled-mapping-2026-09-13.jpg) shows the enabled mapping and updated description. Preferences were closed after capture. The prior fixture tracks remained loaded at their prior positions. No performance MIDI request or playback command was sent during this card.

## Remaining work

Native loading and initial feedback are not proof of playback transitions, cue behavior, sync effects, accurate timing, physical-controller coexistence or musical quality. M11 onward must provide dedicated observations. Production cue/sync host guards remain open. The runtime lease returns to the original keeper job with its original two-hour lifetime and no automatic restart; subsequent tasks must verify the live handles before using it.

> End of autonomously AI-generated implementation evidence.
