# Mixxx wire endpoint acceptance

> Autonomously AI-generated acceptance evidence.

R05's scope is the host decoder/encoder wrapper, with semantic services separate.
The assembled mapping applies the frozen framing to actual native SysEx input
and queued output. It validates native input shape before parser allocation,
uses fixed allowed handler roles, snapshots registrations, freezes decoded
payloads and requires validator success before dispatch. Invalid, unsupported
or faulted input cannot reach a registered setter through this wrapper.
The code installs no production semantic handlers by default.

All required R05 children are accepted: host encoder/decoder, mapping integration,
native clock, native input loss, bounded client sender, native send timer, bounded
host replies and native endpoint validation. Native golden frames verify number,
Unicode and JSON round trips; rejection and conventional coexistence passed on
the current mapping. Capacity, expiry, reset, overflow and cancellation findings
are linked through [native acceptance](R05-ENDPOINT-NATIVE.md).

All 178 software tests, typecheck/build and deterministic mapping generation
passed at the integrated revision. Tests cover unvalidated handlers, malformed
input, stale generations, clock regression, lifecycle cleanup, delayed callbacks
and failure latches. The cleanup regression was demonstrated failing before its
fix. Native output tests do not substitute for these setter and lifecycle checks.

This accepts the specified wrapper scope. R06 onward must supply negotiated
sessions, actual capabilities, authoritative observations, outcomes, bounded
production scheduling and disarm/recovery. M13 still needs cue transport/native
validation. The maximum-message caller deadline limitation is explicit; no
latency SLA, physical MIDI handover or musical release gate is accepted here.

> End of autonomously AI-generated acceptance evidence.
