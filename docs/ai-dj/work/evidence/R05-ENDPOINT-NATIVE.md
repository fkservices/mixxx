# Native endpoint acceptance

> Autonomously AI-generated acceptance evidence.

The current eight-fragment mapping ran in the isolated custom Mixxx host with
its verified monotonic clock and native MIDI sender timer. The final probe
returned exact numeric, Unicode text and closed JSON payloads, plus a fresh
numeric reply after rejection cases. A corrupted checksum produced zero
dispatches; an extra JSON field and an unregistered handler each produced one
rejection. Exactly seven incoming and four outgoing frames matched native logs.
Conventional deck volume changed from MIDI 127 to 64, was observed at 64, and
returned to 127. Play remained zero; no track playback was started.

Previous bounded probes establish native idle/absolute expiry, four-slot
capacity, reset routing, input-loss retirement, maximum request/reply delivery
and queued reply lifecycle. Their original failures remain in the record along
with verified fixes. Maximum host replies require a caller admission budget
that covers encoding and queue time; the default 900 ms admission deadline is
insufficient for the tested maximum payload. Partial-send expiry remains bounded.

[Consolidated record](../runs/R05-ENDPOINT-NATIVE.json),
[maximum reply history](../runs/R05-HOST-FRAGMENT-SENDER.json) and
[reply lifecycle](../runs/R05-REPLY-LIFECYCLE.json) contain hashes and boundaries.
The helper and native host stopped; the profile mapping was restored without
private handlers. Stock Mixxx and original music files were not changed.

Native invalid-input evidence proves codec handlers were not dispatched.
The no-setter property additionally follows from the wrapper's source and the
VM setter regressions. This is not an exhaustive hostile-native-input audit.
Opcode 9 was a private JSON codec fixture, not a production service. No authority,
physical controller, loaded timing, OS recovery or musical acceptance is implied.

> End of autonomously AI-generated acceptance evidence.
