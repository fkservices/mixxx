# F02 evidence: module ownership and validation contract

> This document was autonomously generated by an AI agent at the user's request.

## Handoff

| Field | Value |
| --- | --- |
| Card | F02 |
| Worker start | `2026-09-13T18:48:11.320Z` |
| Evidence captured through | `2026-09-13T18:51:07Z` |
| Source revision at start | `c9aefb3b4da65036ee71497b3ef6a873e3c7daff` |
| Source branch | `codex/ai-dj-plan` |
| Dependency | F01 accepted in the coordinator ledger; `docs/ai-dj/work/baseline.md` reviewed |
| Paths changed | `docs/ai-dj/work/layout.md`; `docs/ai-dj/work/evidence/F02.md` |
| Runtime / exclusive lease | None; exact documentation paths only |
| Installation, build, runtime, or persistent job | None |

F02 completed its documentation scope. It binds a one-way import graph, exact shared-file leases, the F03/F06 integration sequence, and the four package commands F03 must implement and demonstrate. No package, compiler, test, native MIDI, Mixxx, UI, audio, or musical behavior is claimed.

## Inputs and discovery evidence

The worker reviewed `GOALS.md`, the accepted F01 baseline/evidence, `docs/ai-dj/PLAN.md` section 2, `docs/ai-dj/FIRST-BATCH.md`, `docs/ai-dj/WORKER-TASKS.md`, and the F02/F03 cards in `docs/ai-dj/tasks/00-foundation.md` at the source revision above.

The coordinator supplied current Verify-tier graph context for project `mixxx-ai-dj`, generation `2026-09-13T16:44:03Z`, ready with 47,976 nodes and 238,220 edges. Its `File` search for `docs/ai-dj/work/*` returned zero results, with no additional page. Coverage marked the existing baseline/evidence/catalog files `not_tracked`, the proposed layout missing, and the whole work scope unavailable. Therefore no indexed structural claim is made here; direct reads of the named planning documents and catalog exact-path ownership are the evidence source for this documentation-only task.

Current revision and selected toolchain:

```text
$ git rev-parse HEAD
c9aefb3b4da65036ee71497b3ef6a873e3c7daff
$ git branch --show-current
codex/ai-dj-plan
$ node --version
v26.8.1
$ npm --version
11.19.0
```

Package registry reads:

```text
$ npm view typescript version --json
"7.0.2"
$ npm view @types/node version --json
"26.5.1"
$ npm view @julusian/midi version --json
"3.8.1"
```

These reads establish that the selected exact versions were reported by the registry on 2026-09-13. They do not establish download integrity, installation, compilation, ABI compatibility, native import, MIDI port behavior, or Mixxx interoperability. F03 owns TypeScript/Node package proof; F04/F05 own native binding proof.

## Contract validation

| Acceptance item | Result and evidence |
| --- | --- |
| Required modules defined | Pass: `core`, `execution`, `midi`, `hosts/mixxx`, `planner`, `analysis`, `catalog`, `sets`, `diagnostics`, `session`, `local-ui`, `desktop`, and `test` each have a responsibility and import allowance in `layout.md`. |
| Planner excluded from MIDI callbacks | Pass by contract: `midi/` cannot import planner/analysis; the callback can only validate/copy/timestamp/enqueue bounded work. Planner/model work is isolated behind asynchronous data messages. Runtime enforcement remains future proof. |
| MIDI excluded from planner | Pass by contract: `planner/` cannot import `midi/`, `hosts/mixxx/`, `execution/`, native bindings, or callable host functions. |
| History cannot emit performance actions | Pass by contract: `session/` has no transport/native handle and historical projection is read-only. Runtime enforcement remains future proof. |
| Exact package validation commands | Pass as a binding: `npm run typecheck`, `npm run build`, `npm test`, and `npm test -- test/smoke.test.ts`, all from `ai-dj/`; scripts and selector semantics are explicit. These commands were not run because F03 has not created the package. |
| F03/F06 disjoint ownership | Pass: F03 owns manifests/config/smoke; F06 owns `core/actions.ts` and its contract. Coordinator must run integrated typecheck before F06 acceptance. |
| Three workers plus coordinator | Pass: every worker receives exact paths; the third slot requires a compatible catalog card and no lease collision. Coordinator exclusively owns ledger/acceptance/integrated checks. No directory-wide implied ownership exists. |
| Shared entrypoints reserved | Pass: `package-manifest`, `mapping-entry`, `ui-entry`, and composition-root ownership are exclusive and transferred only after accepted handoff. |
| Desktop/local/native-RtMidi defaults preserved | Pass: one local JS/TS desktop package beside Mixxx; `@julusian/midi` is the native RtMidi candidate behind an interface. No other host/device or framework was selected. |
| Upstream automation excluded | Pass: F02 performed no upstream communication, issue/PR action, commit, push, install, source edit, or runtime operation. |

## Checks and remaining proof

The repository planning validators were run after both documents were written:

```text
$ python3 docs/ai-dj/tasks/render.py --check
Checked 14 planning files

$ python3 docs/ai-dj/tasks/plan-execution.py --check
Checked 81 reference batches; 1 accepted; 1 running.

$ python3 docs/ai-dj/tasks/validate.py
PASS: 171 cards, 17 templates, DAG and ownership sequencing, 60 decisions, 66 trace rows, 37 synthetic events and 565 local file links.
PASS: 81 reference batches, resource/dependency checks, 7 checkpoints and full release reachability; 1 accepted tasks.
Expanded DAG: 2 materialized children, 2 accepted. Runtime and musical evidence gates remain required.
```

A local Markdown check found no trailing whitespace, both documents have the required opening and closing AI-generated notices, and every relative Markdown link in the two owned files resolves. These documentation checks cannot prove the commands promised to F03.

F03 must still create the package and demonstrate all four commands on Node 26.8.1, including `.test.ts` discovery, focused selector behavior, strict NodeNext compilation, and build-time relative extension rewriting. F04/F05 must install/import the candidate native binding and prove its macOS ARM behavior. Later cards must enforce the import/callback rules with actual source, tests, process boundaries, and runtime evidence.

No blocker or continuation child was found within F02's documentation scope. Coordinator review and ledger acceptance remain external to this worker handoff.

Contract: [module ownership and validation contract](../layout.md). Card: [F02](../../tasks/00-foundation.md#f02-freeze-module-ownership-and-worker-validation-commands).

> End of autonomously AI-generated document.
