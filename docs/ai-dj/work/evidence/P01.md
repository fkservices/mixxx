# P01 — Playlist occurrence and set contracts

> Autonomously AI-generated contract acceptance evidence at the user request.

Source revision: `0945e0b`. The new `ai-dj/core/playlists.ts` and `ai-dj/core/sets.ts` define coherent versioned playlist reads, library-scoped track identity, distinct set occurrences, planned order and separate actual playback history, sections and meaningful-participation policy, pins and duplicate notices, terminal accounting, mode permissions, explicit saves and source-edit reconciliation. They reuse F06 identity/evidence types and the established R15 mode names.

[The normative contract](../contracts/sets.md) specifies exact occurrence-multiset preservation, evidence requirements and runtime handoff. `[A, B, A]` becomes `[a1, b1, a2]`; reordering to `[a2, a1, b1]` retains all three obligations. Preview/loading is not played. Confirmed unavailability skips with evidence/reason and advances only the logical cursor; ambiguity and unknown results remain unresolved. Source edits do not silently delete obligations; manual external tracks do not enable free AI selection or satisfy a different occurrence.

## Validation

- Integrated TypeScript typecheck and build passed after the final contract edits. Both generated modules contain only erased declarations/export markers; no MIDI, file, network, persistence or execution behavior is introduced.
- A private fixture under the local `jobs/p01-contracts` directory compiled under strict NodeNext with unchecked-index and exact-optional checks, then executed the A/B/A permutation example. It also distinguished a duplicated-identity substitution. This is a contract example, not a production reorder validator.
- Eight consumed `@ts-expect-error` cases prove that loaded, preview and unknown results cannot be assigned as terminal; played needs participation; timeout is not confirmed-unavailable; Playlist Only cannot grant performance permission; partial reads cannot be complete snapshots; and completion records cannot contain loaded outcomes.
- The existing suite passed 192/192 tests, with zero failures, skips or cancellations. Those tests establish regression coverage for existing modules, not new runtime playlist behavior.
- Reviewed identity reuse, incomplete reads, exact planned/admitted multisets, pins, duplicates, section coverage, confirmed-unavailable versus unknown, terminal reconciliation, manual replacement and versioned save/source-edit rules against the discovery/defaults and accepted F07/R15 contracts.
- Graph project `mixxx-ai-dj` is at this checkout, generation `2026-09-13T16:44:03Z`. A bounded core search returned zero results; exact coverage reports the relevant core files untracked or missing. Source reads supplied fallback evidence; no graph completeness claim is made.

P01's types-and-invariants scope is accepted. Runtime payload validation, state transitions, native read/load/save, persistence, planner integration and actual meaningful-playback accounting remain for subsequent playlist cards. No native playlist or audible-mixing result is claimed.

> End of autonomously AI-generated contract evidence.
