# R03 SysEx encoder acceptance

> Autonomously AI-generated implementation evidence; no live-send claim.

Coordinator implementation from revision `52a746b`, against [R01 framing](../contracts/wire.md). [The encoder](../../../../ai-dj/midi/sysex-encode.ts) creates complete frame arrays without owning or calling a transport. It validates the exact input fields, directions/opcodes, session-token spelling and safe-integer sequence before encoding. Diagnostic opcodes require explicit opt-in and are disabled by default. Frame construction is all-or-error; it never returns a partial message on an invalid payload.

Finite binary64 and strict Unicode text match the frozen diagnostic frames byte-for-byte. JSON is accepted as plain object data and serialized using descriptors into a bounded byte-counted string, without calling getters or `toJSON`. Proxies, cycles, accessors, hidden/symbol properties, sparse/extended arrays, nonfinite values, negative zero, non-JSON values and malformed Unicode reject. A JSON string supplied as the root is rejected; duplicate keys cannot be introduced through a raw-text parsing shortcut. This encoder does not parse incoming JSON or validate operation-specific schemas.

Packing resets at each 512-byte fragment. Every header field and packed payload byte is covered by CRC-16/CCITT-FALSE. The 128-bit session uses BigInt arithmetic and the 53-bit sequence is range-checked before conversion, avoiding JavaScript bitwise truncation. The largest payload is 65,536 decoded bytes, 128 frames of at most 636 bytes. Returned frames contain no references to mutable input objects. The outer frame array is frozen; byte arrays are caller-owned output buffers.

## Validation

Four focused tests cover:

- Both exact retained numeric/Unicode golden frames, including CRC digits.
- Lengths 0, 1, 7, 8, 511, 512, 513 and 65,536; rejected 65,537; complete payload recovery using a separate test unpacker; maximum session/sequence digits; and a multibyte character split across fragments.
- Plain JSON round trip, exact 65,536-byte JSON boundary, depth 32 accepted/33 rejected, 4,096 value nodes accepted/4,097 rejected, and rejection without invoking input hooks.
- Invalid header fields, extra fields, unsupported opcode/direction combinations, nonfinite/negative-zero numeric input and disabled diagnostic defaults.

The integrated suite passed **105 tests**. Typecheck and build exited zero. R01's retained vectors supply independent expected bytes; the test unpacker does not import encoder internals. Graph generation `2026-09-13T16:44:03Z` contains no coverage for these new paths, so the coordinator reviewed exact source and tests directly.

## Remaining scope

R03 is accepted as an encoder only. Session-zero and sequence-zero values can be represented by framing but cannot authorize actions; lifecycle/schema/authority validation must reject inappropriate use before sending. Incoming parsing/reassembly, protocol lifecycle, trusted host identity, dispatch, actual Mixxx SysEx binding, timing and musical behavior remain later work. No running mapping or native service was changed or sent a command by these checks.

> End of autonomously AI-generated evidence.
