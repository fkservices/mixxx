# M06: play and volume mapping fragment

> Autonomously AI-generated implementation evidence at the user's request.

Input revision `a5fc143`; M05 accepted. Coordinator implementation; no runtime lease or native mapping change.

[Fragment](../../../../ai-dj/hosts/mixxx/fragments/play-volume.js) registers the fixed play-volume module. It accepts only B0/B1 command statuses with matching zero-based channel and Channel1/Channel2 group, play address 20 or volume address 21 (hex), and integer data 0–127. Play uses desired `setValue(..., "play", 0|1)`; volume uses `setParameter(..., "volume", data/127)`. It never toggles play, presses cue, emits MIDI feedback or resets values at shutdown.

Initialization probes only the four profile-declared controls with `makeConnection`; acquired handles belong to the common lifecycle. Missing/failed probes disable that route. A route writes only while its connection reports `isConnected === true`. Zero-valued control reads do not determine availability. Shutdown drops fragment references before common connection disposal. Reinitialization performs a fresh probe, with no automatic command replay.

Mixxx 2.5.6 source verifies the connection API: [makeConnection implementation](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp#L232) returns an undefined value when the control cannot be resolved and a connection proxy on successful registration. The [proxy](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/controllers/scripting/legacy/scriptconnectionjsproxy.h) exposes isConnected. This is local connection evidence, not proof of current track identity, audibility, semantic authority or dynamic topology completeness. Future independent host presence/freshness reconciliation remains required.

## Validation

[Five tests](../../../../ai-dj/test/host/play-volume.test.ts) exercise repeated on/off and positive CC values on both decks, every volume byte, unavailable and zero-valued controls, disconnected connections, explicit reprobe after reinit, shutdown suppression, wrong channel/group/address/status, feedback rejection, malformed values and a probe exception isolated to one route. No cue keys or MIDI output appear in writes.

The [harness](../../../../ai-dj/test/host/harness.ts) now models isConnected, undefined results for explicitly unavailable controls, and explicit simulated disconnects. M06 ownership was extended to this existing test helper; its earlier lifecycle tests remain passing. This fixture mechanism does not claim that arbitrary live host deletion automatically updates a proxy.

Final checks with the local Node toolchain:

| Check | Result |
| --- | --- |
| `npm test` | 73 passed, including five new fragment tests |
| `npm run typecheck` | Passed |
| `npm run build` | Passed |

Initial strict checking found a recursive getter inference and a tuple-spread typing issue in tests; explicit return typing and indexed tuple arguments corrected them. Final integrated tests, planning/render validation and whitespace checks passed.

Graph discovery found the connection implementation and proxy. Coverage identified partial ranges in the source interface/proxy; relevant definitions and reported ranges were read directly, then verified against the released tag. New fragment/harness coverage was unavailable. No exhaustive graph assertion was used.

## Remaining verification

The fragment is not yet assembled into the running mapping. M10 integrates it; M12 and subsequent live checks must prove actual Mixxx play/volume changes, feedback, UI, audio and timing. Connection existence is not permission to play an empty or human-controlled deck. The semantic executor must check current binding, ownership, mode and disarm before sending raw commands.

> End of autonomously AI-generated implementation evidence.
