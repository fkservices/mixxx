# O07-IPC — Bounded recorder worker transport

> Autonomously AI-generated implementation evidence at the user's request.

Source base `f051c378a3`. `SessionJournal` owns a real Node worker and accepts pre-encoded, already-admitted event JSON through synchronous `offer`. That method never awaits filesystem work. Default capacity is 1,024 pending/in-flight records and 4 MiB input bytes, with hard configurable maxima of 8,192 records and 16 MiB. Encoded records reserve space beneath the chunk writer's 256 KiB cap. One unacknowledged packet bounds IPC independently; the worker rejects a second simultaneous command. Disk append, JSON parsing, producer tracking and sealing execute in the worker. A 5-second default deadline bounds startup/ack waiting; failure terminates the worker and marks capture incomplete.

At capacity, calls return omitted immediately and retain only one bounded range of omitted ingress offers. Pending older records drain first, then the worker persists a capture-gap plus exact ingress-offer range in a degraded-state record. That range describes calls at the recorder boundary, not fabricated producer sequence numbers. The gap's producer loss is explicitly unknown. The next available producer event independently establishes any known producer sequence gap. Repeated raw bytes at distinct producer sequences remain distinct events. Recorder sequence and ingest clock are assigned in the worker; producer clock and original event identity remain unchanged.

The worker tracks at most 256 producer/stream/epoch tuples. Sequence jumps produce explicit known-range gaps; regression rejects the input and retires capture. This is not full event identity/payload/reference validation: producers must supply admitted O01 events with globally unique IDs. Import validation and capture-boundary proof remain separate. Worker acknowledgement means appended to the open chunk, not fsynced durable data; only final seal/close acknowledges synchronized storage. No status reports complete physical capture: healthy status remains unknown until scope evidence exists.

Close drains queued records and omission markers, writes an ended recording event, seals storage and acknowledges closure. Recording ending never implies music stopped. Worker errors and abort do not add a clean end marker. `ChunkWriter.abort` closes its handle without sealing or ending, so recovery exposes an unsealed tail. Once failure occurs, queued/in-flight events may be unconfirmed; the status is failed/gaps-present, and exact durable extent must be recovered from disk rather than inferred from accepted offers.

## Validation

Five real-worker tests passed: 20 distinct raw events retain contiguous recorder order; saturation with one credit records 999 omitted offers and bounded queue state; producer discontinuity is recorded while regression fails; a real filesystem seal conflict surfaces failure; terminating the actual worker preserves complete lines with uncertain-tail recovery. Full package: **242 passing tests**; TypeScript build passed. Initial build found an unknown-typed Worker error argument; the handler now normalizes it explicitly. [Run record](../runs/O07-IPC.json) retains hashes and final test summary.

These tests exercise actual Node workers and files, not a mock disk transport. They do not establish live MIDI timing, actual ENOSPC, pause/resume snapshot correctness, dense six-hour capacity, full runtime payload admission or whole-system capture fidelity. Main-thread offer still performs bounded byte sizing and structured-clone submission; no hard realtime claim is made. O07-WORKER is split into this accepted transport and O07-LIFECYCLE. The latter preserves all outstanding pause, degradation, snapshot, disk-full and responsiveness requirements; O07 remains incomplete. Graph discovery returned no recorder symbols and coverage was unavailable, so current source and O01 contracts supplied evidence.

> End of autonomously AI-generated implementation evidence.
