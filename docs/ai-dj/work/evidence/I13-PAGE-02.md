# I13 — playlist mutation inventory, page 2

Autonomously AI-generated source discovery evidence. This accepts a bounded page, not the family or executable MIDI support.

[Page 2](../inventory/catalog-selection/page-02.json) adds twelve distinct provisional candidates. [Manifest 2](../inventory/catalog-selection/source-manifest-02.json) pins current source revision `e580c20e08`, full-file hash, exact function ranges and snippet hashes. The inherited page-1 context identifies the historical comparison target only; no native app was opened or host session revalidated, and source-to-installed-binary equivalence remains unknown.

| Candidate group | Finding that constrains the service |
| --- | --- |
| Rename | A successful UPDATE and renamed signal do not prove the requested playlist row existed. |
| Single lock, category lock, batch lock | Single lock does not check affected rows; batch reports requested set size. Category read failure is indistinguishable from an empty category. |
| Single append, batch append | Wrapper delegation is a possible parameterized alias, not yet a shared admitted service binding. The append path does not check active transaction or commit outcome. |
| Remove by track ID | Targets all matching occurrences; must remain distinct from removing one source occurrence. |
| Remove one/multiple positions | Mutable positions need occurrence/revision guards. A descending position list is not deduplicated; repeated positions may address shifted successors. Void results and signals alone are not acknowledgements. |
| Single/batch insert | The corrected batch path has committed-row accounting; single insertion still has different timestamp and failure behavior. Do not claim equivalence. |
| Reorder | Corrected source preserves complete occurrence rows and respects locks, but callers still supply current positions and receive no explicit success result. |

All command, feedback, native-UI and runtime dimensions remain untested. Availability is unknown. Singleton denotes a provisional host-library service; argument identities and runtime target enumeration remain unresolved. Native unit evidence for prior corrections is retained separately and does not upgrade these MIDI assessments. Page-1 ordered entries and DISTINCT membership remain separate candidates.

Validation used the inventory JSON Schema with format checking, plus unique IDs across pages 1/2, matching inherited contexts, row limit, continuation linkage and exact source/snippet hash checks. Twelve rows fit the twenty-row limit. Source coverage is bounded: graph search returned 15 selected method symbols without truncation at generation `2026-09-13T16:44:03Z`; coverage was partial/stale, so current selected method bodies were read directly, including the current reorder region overlapping the recorded gap. Unrelated missed ranges are outside this page's claims.

`I13-PAGE-03` continues from cursor `I13-page-03`: remaining mutations and alias decisions, stable selected-track identity, sidebar/table native menus, matching-version manual, runtime target enumeration and source/binary reconciliation. Historical menu evidence is carried forward without a new observation claim. Four-source discovery closure is not satisfied; I13 and I23 remain open.

End of autonomously AI-generated page-2 evidence.
