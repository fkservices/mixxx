# O01 — Session event and clock contract

> Autonomously AI-generated contract evidence at the user's request.

Added `ai-dj/core/session.ts` and [the normative session contract](../contracts/session.md). The envelope identifies recording, producer/device stream and epoch, source and ingest sequences/clocks, host context, track/occurrence, actor evidence and causal links. The payload union includes raw MIDI, decoded gestures, AI intent, full semantic actions/outcomes, display stages, host observations, mode/ownership, playlist/plan/history, clock calibration and gaps.

Embedded decoder profiles define channel/address/scaling/polarity, relative encodings, CC14 pairing and note release interpretation. Chunk manifests distinguish NDJSON storage from versioned JSON export. Visual snapshots carry stream cursors and open gaps. Export and snapshot data are marked history-only/visualization-only; the contract explicitly requires actual output-path isolation rather than relying on those tags.

The reviewed volume example preserves two input gestures and a separate observation; a matching observed value does not establish who caused it. The relative jog example demonstrates that byte 127 means -1 in signed seven-bit encoding and +63 in binary-offset encoding. Profile identity is mandatory for semantic decoding. Shared gesture IDs prevent counting raw bytes, decoder fan-out and observations as additional physical gestures. Native capture support is not inferred.

Validation: the complete TypeScript build passed. A synthetic arithmetic/grouping check verified increasing normalized volume, the two relative interpretations and distinct gesture-ID counting. Emitted session JavaScript contains only `export {}` and a closing comment, confirming this is a data contract with no recorder or control behavior. Graph coverage was unavailable for these new TypeScript files; related action/state/mode/set definitions were inspected directly.

Acceptance is limited to O01's event-envelope, causal-ID, snapshot, clock and journal/export contract scope. Runtime validators, producer hooks, the recorder worker, persistence bounds, export/import, timeline UI, physical-controller capture and live/history equivalence remain their own tasks. No full runtime regression suite was rerun for erased declarations, and no musical/MIDI result is claimed.

> End of autonomously AI-generated contract evidence.
