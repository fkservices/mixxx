# F04: candidate native MIDI installation

> This document was autonomously generated by an AI agent at the user's request.

F04 reached a terminal package-install result on integrated revision `b5dc433cadee8def071705967aa893b014f21883`. It pins `@julusian/midi` to exact version `3.8.1` in the isolated `ai-dj` package and records the complete terminal job in [native-install.json](../jobs/native-install.json). This is installation and identity evidence only, not native-load or MIDI-operation evidence.

## Candidate and runtime

Immediately before the install, npm registry metadata for `@julusian/midi@3.8.1` reported Node engine `>=14.15`, the package tarball and integrity captured in the job record, and the package README described common-platform prebuilds and macOS CoreMIDI support. That makes it the recorded initial candidate under F02; it does not establish compatibility with Node 26.

The command ran from `/Users/faizkhalid/Documents/ChatGPT/DJ_app/ai-dj` using `/Users/faizkhalid/.local/bin/npm`:

```sh
/Users/faizkhalid/.local/bin/npm install --save-exact @julusian/midi@3.8.1 --no-audit --no-fund
```

The frozen runtime was Node `v26.8.1` at `/Users/faizkhalid/.local/bin/node`, npm `11.19.0`, macOS `27.0`, and `arm64`. The command started at `2026-09-13T19:00:51Z`, ended at `2026-09-13T19:00:52Z`, and exited `0`. It added four packages. `package.json` and `package-lock.json` now specify the exact production dependency and its transitive lock entries.

## Install-script handling and identity checks

The initial command emitted an npm 11 install-script warning for `@julusian/midi`'s documented verifier/fallback build script. F04 recorded that output and then ran the exact-version approval command recorded in the job. The follow-up exited `0`; the manifest contains `allowScripts["@julusian/midi@3.8.1"]: true`, and `npm install-scripts ls` subsequently reported no unreviewed install scripts.

The installed dependency tree reports `@julusian/midi@3.8.1`. A file-system-only identity check found its `midi-darwin-arm64/node-napi-v7.node` prebuild and classified it as a Mach-O 64-bit arm64 bundle; its SHA-256 is in the job record. That check did not load or execute the addon.

## Boundary and handoff

F04 did not import the package, construct a MIDI input/output, enumerate endpoints, open/create a port, launch Mixxx, configure devices, or use music. The terminal install result and a matching arm64 prebuild do not prove that the addon loads under Node 26.8.1.

F05 should use the recorded runtime and terminal job to import the binding, then enumerate available endpoints without opening a performance port. No runtime-version change is proposed.

> End of autonomously AI-generated document.
