# R01 framing acceptance

> Autonomously AI-generated contract evidence; no runtime implementation claim.

The coordinator completed [extended wire framing v1](../contracts/wire.md) from accepted F08/F09 inputs on revision `c04486c`. This task owns the framing document only. No mapping, service endpoint, audio configuration or live MIDI job was changed.

## Review and arithmetic checks

The layout has 46 bytes before the packed payload, followed by three checksum digits and one terminator. Payload packing adds one mask per seven decoded octets. Thus an empty fragment is 50 bytes, and 512 decoded bytes produce `50 + 512 + 74 = 636` transmitted bytes. Maximum 65,536-byte messages require exactly 128 fragments. Four simultaneous messages retain at most 262,144 decoded bytes per ingress endpoint, plus explicitly bounded metadata and the partial-frame buffer.

The numeric fixture is binary64 little-endian `00 00 00 00 00 00 F8 BF`, or -1.5. The first seven octets pack with mask `40`, restoring `F8` from `78`; the last octet uses mask `01`, restoring `BF` from `3F`. CRC `D127` packs to `27 22 03`: `39 + 34*128 + 3*16384 = 53543`.

The Unicode fixture `é🎧` has six UTF-8 octets `C3 A9 F0 9F 8E A7`. All high bits are one, giving mask `3F` followed by `43 29 70 1F 0E 27`. Reconstructed UTF-8 decodes exactly to the original string. CRC `08B0` packs to `30 11 00`: `48 + 17*128 = 2224`.

An independent Python check generated the frames using `struct.pack` and `binascii.crc_hqx`, then verified them with a separate bitwise CRC loop and mask-based unpacker. Both exact frames round-tripped, with lengths 60 and 57. The standard CRC check input `123456789` returned `29B1`. Additional raw-packing length checks passed at 0, 1, 7, 8, 511 and 512 octets; these arbitrary bytes test packing, not UTF-8 validity. Maximum-session and maximum-safe-sequence digit calculations confirmed last-digit limits 3 and 15. This temporary calculation is supporting arithmetic evidence, not a checked-in production codec or parser test suite. R03/R04 must implement and independently verify the retained golden frames.

## Rejection review

The contract gives finite outcomes for foreign prefixes, unknown versions/opcodes, direction/encoding mismatches, truncated and oversized frames, noncanonical digits/masks, corrupt checksums, inconsistent/duplicate fragments, slot exhaustion, expiry and invalid payloads. No partial payload reaches a handler. Frame timeout is absolute; message reassembly has both absolute and idle limits, with duplicates unable to refresh either. Strict schema validation, sequence replay policy and authority admission remain mandatory downstream steps. Malformed input cannot request evaluation, arbitrary function dispatch or an automatic reply loop.

## Source and limitations

The [MIDI Association identifier policy](https://midi.org/new-midi-association-sysex-id-policies-as-of-oct-15-2025), read on 2026-09-13, distinguishes local development `7D` from identifiers suitable for public products and excludes custom SysEx from `7C`. The contract records that release boundary without claiming an assigned manufacturer identity.

Markdown lint and project documentation validation passed. R01 is accepted as a byte-format decision with checked numeric and UTF-8 vectors. Actual SysEx routing, parser resource enforcement, acknowledgements, replay handling, host identity, reliable control, performance and latency remain unverified implementation work under R02 onward and the native gates.

> End of autonomously AI-generated evidence.
