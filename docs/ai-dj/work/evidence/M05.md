# M05: mapping bootstrap and host harness

> Autonomously AI-generated implementation evidence at the user's request.

Input revision `e29804e`; F03/F09/F11 accepted. Coordinator execution. Existing Node keeper PID 73352 and isolated Mixxx PID 73988 were verified before transferring the runtime lease. Neither process was restarted or duplicated.

## Implementation

[XML registration](../../../../res/controllers/AI-DJ.midi.xml) declares exactly the frozen 11 command routes, all script-bound to the single `AIDJ.input` entrypoint. Feedback/presence routes are not input commands. [Bootstrap](../../../../res/controllers/AI-DJ-scripts.js) defines a fixed fragment order: play-volume, fader-cue, sync, conventional-feedback. M10 inserts those fragments between the explicit assembly markers. Each registers one fixed name with init/shutdown/input methods. No performance fragments are installed by M05.

Initialization cleans the prior lifecycle; partially initialized modules are unwound in reverse order. Registered timers and connections are disposed independently even after a cleanup exception. Cleanup failure stays latched and prevents reinitialization in that script instance. Registration rejects duplicates, unknown names and active mutation. Fragments must register every acquired handle and independently match their frozen packet route.

[Harness](../../../../ai-dj/test/host/harness.ts) exposes deterministic value/parameter stores, writes, connections, timers, packets and logs. It executes bootstrap and supplied fragments in a bounded Node VM. It is a host API stub, not an audio engine or security sandbox. [Lifecycle tests](../../../../ai-dj/test/host/harness.test.ts) are a separate discoverable file so later fragment imports do not register duplicate tests. M05 ownership was extended to include that test file.

## Verification

All 68 package tests, strict typecheck and build passed. Four lifecycle cases prove two initialization/shutdown cycles with no handles or writes, fixed ordering despite reversed registration, cleanup on reinitialization and partial-init failure, rejection of invalid registration, and continued cleanup plus latched refusal after a shutdown exception. XML parsing and exact comparison against the profile verified all 11 unique status/address/group tuples.

Native CUA selected the new AI DJ mapping in the isolated Mixxx 2.5.6 profile. Actual controller console output records two successful `AIDJ.init` completions and an intervening `AIDJ.shutdown`, including the final script reload:

```text
Executing "AIDJ" . "init"
AI DJ shutdown; cleanup=complete
AI DJ initialized; fragments=0; profile=mixxx-2.5.6-latenight-conventional-v1
Executing "AIDJ" . "shutdown"
AI DJ shutdown; cleanup=complete
Executing "AIDJ" . "init"
AI DJ shutdown; cleanup=complete
AI DJ initialized; fragments=0; profile=mixxx-2.5.6-latenight-conventional-v1
```

The final reload followed the source update adding the cleanup-failure latch. Native enable/disable selections were also exercised, but the Apply logs alone are not counted as additional controller lifecycle cycles. The exact two-cycle resource-leak assertions are harness evidence; the native bootstrap currently creates no handles. The controller console reported file-watcher warnings for common and AI DJ scripts during reload; loading and initialization completed, with no script exception or cleanup failure. Future hot reload reliability is not claimed. The main log was buffered; the live controller console supplied the complete record.

Installed and repository file bytes matched:

- Script SHA-256: `701be666c8e63d941821b6f375296beaa6440bf2698845c3b7e3b9b3ae660f26`
- XML SHA-256: `48c25365dd41c88cd067cf225388c02531c83e82ebdfc2c8a37e292a37a327b7`

[Actual native screenshot](../screenshots/mixxx-mapping-bootstrap-2026-09-13.jpg) shows AI DJ enabled with the new XML/script names. Preferences were saved/closed; both preexisting fixtures remained loaded at their prior positions. No playback or performance MIDI command was issued.

## Remaining work

M06–M09 implement the fixed fragments and M10 assembles them; actual performance changes, feedback, presence, timing, guard support and musical behavior remain unverified. Raw mapped commands will require independent semantic/executor authority before production dispatch. The runtime lease returns to the existing keeper job, whose original two-hour lifetime remains unchanged. Controller connection remains deferred by the user.

Graph lookup was provisional and paginated only for locating existing mapping examples; no exhaustive graph claim was made. Coverage for these new files was missing in generation `2026-09-13T16:44:03Z`; direct profile, existing fixture XML and source inspection supplied the evidence.

> End of autonomously AI-generated implementation evidence.
