# F09 evidence: conventional MIDI contract

> This document was autonomously generated by an AI agent at the user's request.

## Scope and result

Prepared for coordinator review against integrated input `0ef0499db28c6c86bb7c75e390665da831f8c20c`, with F01–F07 accepted. Owned edits are only [conventional.md](../contracts/conventional.md), [conventional-profile.json](../../../../ai-dj/hosts/mixxx/conventional-profile.json), and this evidence. No runtime/device configuration, mapping implementation, Mixxx launch, commit or acceptance was performed.

The specification includes five families, nine instances, 11 command addresses including both cue releases, nine value feedback addresses and nine presence addresses. It selects sync lock enabled as the sync operation, reserves its bytes, and explicitly gates semantic execution on a closed-contract extension. Cue press/release bytes are specified without claiming conventional MIDI proves a particular F06 lease. The Node Output command / Node Input feedback topology matches the accepted integration correction.

## Source verification

Graph Verify tier: project `mixxx-ai-dj`, root `/Users/faizkhalid/Documents/ChatGPT/DJ_app`, generation `2026-09-13T16:44:03Z`, ready, 47,976 nodes / 238,220 edges. Exact snippets were read for `SyncControl.slotSyncEnabledChangeRequest`, `CueControl.cuePreview`, `EngineBuffer.requestEnableSync`, and `ControllerScriptInterfaceLegacy.setParameter`. Both-direction traces were requested for the first two; outbound trace for `requestEnableSync`. The graph omitted signal/call relationships and included an unrelated `RubberBandWrapper.isValid` heuristic edge for cue; those were not treated as proof. The broad relevant-name search returned 23 entries, consumed in pages 20+3 with `has_more:false`; subsequent focused searches returned 1/2 results or zero without completeness claims. The parent's sync-location evidence was treated as a starting pointer only.

Coverage on all candidate source paths was checked. Most were `no_recorded_issue`, `metadata_match`; this is not proof of completeness. Reported partial ranges were read directly: `cuecontrol.cpp` 1024,1030,1180,2413,2535,2596,2602; `enginebuffer.cpp` 103,106,414,569,758,814,816,1218,1411,1593–1691 (including surrounding relevant play handler); `controllerscriptinterfacelegacy.cpp` 330,405; `enginesync.cpp` 153. New profile/doc paths were `coverage_unavailable`, missing; direct creation/read-back supplies their evidence. No exhaustive codebase or negative-callgraph claim is made.

The checkout is newer than the selected release. Material semantics were independently read from **official tag `2.5.6`** through HTTPS raw source and the official 2.5 manual. Python urllib initially failed local certificate validation; curl subsequently downloaded the files with TLS verification enabled. An initially guessed old scripting path returned HTTP 404; the correct tagged `src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp` succeeded. No certificate bypass or runtime action was used.

| Official tagged file | Material finding | SHA-256 of downloaded source |
| --- | --- | --- |
| [src/engine/enginebuffer.cpp](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/engine/enginebuffer.cpp#L413) | Boolean sync request and enable/disable one-shot queue; lines 776–791 verify explicit play requests | `ab066eb911dab91f33d4f1ff87d6c31aacc2c3da413638c9e0184c2f9be9f256` |
| [src/engine/sync/synccontrol.cpp](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/engine/sync/synccontrol.cpp#L144) | Actual sync confirmation, vinyl/passthrough effects; lines 511–524 handle boolean request | `89fb09a465dc8b721dc4ecf7ce0ba8ed2af642cc0ab6bf24832ef91bed2b9ca7` |
| [src/engine/sync/enginesync.cpp](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/engine/sync/enginesync.cpp#L33) | Follower request can select a soft leader and change participating decks | `3a105d558fe2030f285734588843e73865716d10cf520bba716ca82baccd142e` |
| [src/engine/controls/cuecontrol.cpp](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/engine/controls/cuecontrol.cpp#L1385) | Main cue preview press/release behavior and invalid-marker early return | `12a07523741a4f653271c7c3bbcef2022c82803ad5391144c32ab826257f942f` |
| [src/engine/enginemixer.cpp](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/engine/enginemixer.cpp#L888) | Volume taper -20/0 dB and neutral 1; line 104 crossfader range -1..1 | `fc0fb81db8a4fe7cb9e2bdf67d32a42262533d84a1dbea0df018670cc64e4783` |
| [src/control/controlbehavior.cpp](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/control/controlbehavior.cpp#L70) | Exact centered potmeter MIDI conversion and audio taper parameter conversion | `5436c4286aec17b649ee7ce22c5644244a6d60772151c8d1cc5a8ce77fa899f6` |
| [src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp](https://github.com/mixxxdj/mixxx/blob/2.5.6/src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp#L134) | Missing controls return zero; explicit setValue/setParameter; makeConnection validates existence | `437aa1eb560c62c647254f01020f5b35609721821da4884aa52c51fb59b1cfa8` |

The [official 2.5 controls reference](https://manual.mixxx.org/2.5/en/chapters/appendix/mixxx_controls) corroborated play/play_indicator distinction, cue_preview, volume/crossfader, sync_enabled and sync_mode. Source behavior, not a manual button description, determines the scripted desired-state binding. Source links identify released semantics, not local runtime results.

## Reproducible bounded profile validation

From the repository root, run the following standard-library Python snippet. This checks static schema/shape, fixed address reservations, 27 vectors, all 128 quantized values for each continuous curve, 65,538 continuous test inputs across both curves, and 512 release packets across both cue channels. It is not a runtime JSON admission implementation or host test.

```python
import json, math
from pathlib import Path

def unique_object(pairs):
    out = {}
    for key, value in pairs:
        assert key not in out, ('duplicate JSON key', key)
        out[key] = value
    return out

def shape(obj, keys):
    assert type(obj) is dict and set(obj) == set(keys.split()), (obj, keys)

def bounded(value, depth=0):
    assert depth <= 10
    if type(value) is dict:
        assert len(value) <= 32
        for key, child in value.items():
            assert type(key) is str and len(key) <= 128
            bounded(child, depth + 1)
    elif type(value) is list:
        assert len(value) <= 32
        for child in value:
            bounded(child, depth + 1)
    elif type(value) is str:
        assert len(value) <= 512
    elif type(value) in (int, float):
        assert math.isfinite(value)
    else:
        assert value is None or type(value) is bool

raw = Path('ai-dj/hosts/mixxx/conventional-profile.json').read_bytes()
assert len(raw) <= 65536
p = json.loads(raw, object_pairs_hook=unique_object)
bounded(p)
shape(p, '_aiDisclosureStart schemaVersion profileId status runtimeEvidence baseline transport feedbackPolicy scalings controls _aiDisclosureEnd')
assert p['schemaVersion'] == 1 and p['status'] == 'specification-only'
assert p['runtimeEvidence'] is False
assert p['baseline'] == dict(host='mixxx', version='2.5.6', platform='macos-arm64', skin='LateNight', theme='PaleMoon', deckCount=2, configurationId='mixxx-2.5.6-latenight-f01')
assert p['transport'] == dict(protocol='midi-1.0-channel-voice-3-byte', logicalName='AI DJ', commandEndpoint='node-output', feedbackEndpoint='node-input', independentHandles=True, pairingEvidence='required-F11', feedbackEcho=False, requestIds=False, deduplication=False, actorAttribution=False, midiClockSynchronization=False)
assert p['feedbackPolicy'] == dict(initialBurst='presence-before-value-per-control', refreshIntervalMs=100, expireAfterMs=500, presenceValues=dict(unavailable=0, unknown=64, present=127), receiptTimeIsHostTime=False, snapshotAtomic=False, knownIdentityFromMidi=False, productionObservation='requires-external-context-and-freshness-evidence')
assert len(p['controls']) == 9 and len(p['scalings']) == 4
curves = {s['id']: s for s in p['scalings']}
assert len(curves) == 4 and set(curves) == {'boolean-nonzero', 'note-gate', 'volume-travel-7bit', 'crossfader-centered-7bit'}
for s in curves.values():
    shape(s, 'id decode encode normalizedTolerance')
assert curves['volume-travel-7bit']['normalizedTolerance'] == 1/254
assert curves['crossfader-centered-7bit']['normalizedTolerance'] == 1/252

def decode(curve, value):
    if curve == 'volume-travel-7bit':
        return value/127
    if curve == 'crossfader-centered-7bit':
        return value/128 if value <= 64 else (value-1)/126
    assert curve in ('boolean-nonzero', 'note-gate')
    return int(value > 0)

def encode(curve, value):
    if curve == 'volume-travel-7bit':
        return math.floor(127*value + 0.5)
    if curve == 'crossfader-centered-7bit':
        return math.floor((128*value if value <= 0.5 else 126*value+1) + 0.5)
    return 127 if value else 0

expected = {}
for deck in (1, 2):
    for family, address, presence, key, curve in (
        ('play', 0x20, 0x70, 'play', 'boolean-nonzero'),
        ('volume', 0x21, 0x71, 'volume', 'volume-travel-7bit'),
        ('sync', 0x22, 0x73, 'sync_enabled', 'boolean-nonzero'),
        ('cue', 0x23, 0x72, 'cue_preview', 'note-gate'),
    ):
        expected[f'deck-{deck}.{family}'] = (deck, address, presence, key, curve)
expected['mixer.crossfader'] = (None, 0x24, 0x74, 'crossfader', 'crossfader-centered-7bit')
assert {c['id'] for c in p['controls']} == set(expected)
commands, feedback = set(), set()
vector_count = 0
for c in p['controls']:
    shape(c, 'id capabilityId deck host command feedback presence semantic examples')
    deck, address, presence, key, curve = expected[c['id']]
    cue = curve == 'note-gate'
    sync = key == 'sync_enabled'
    continuous = key in ('volume', 'crossfader')
    cs = (0x90 if cue else 0xb0) + deck-1 if deck else 0xb2
    fs = 0xb8+deck-1 if deck else 0xba
    assert c['deck'] == deck
    assert c['host'] == dict(group=f'[Channel{deck}]' if deck else '[Master]', writeKey=key, readKey=key, writeApi='setParameter' if continuous else 'setValue', readApi='getParameter' if continuous else 'getValue')
    assert c['command'] == dict(status=cs, address=address, releaseStatus=0x80+deck-1 if cue else None, encoding=curve)
    assert c['feedback'] == dict(status=fs, address=address, encoding='normalized-7bit' if key == 'volume' else 'crossfader-centered-7bit' if key == 'crossfader' else 'boolean-0-127', meaning='control-signal-only' if cue else 'host-control-state')
    assert c['presence'] == dict(status=fs, address=presence)
    shape(c['semantic'], 'status action releaseAction parameter operationSpecId')
    action = None if sync else 'deck.cue_press' if cue else 'mixer.set_parameter' if key == 'crossfader' else 'deck.set_parameter' if key == 'volume' else 'deck.set_playing'
    assert c['semantic'] == dict(status='extension-needed' if sync or cue else 'specified', action=action, releaseAction='deck.cue_release' if cue else None, parameter=key if continuous else None, operationSpecId='mixxx.sync-enabled.v1' if sync else 'mixxx.cue-lease-guard.v1' if cue else None)
    for status in [cs] + ([c['command']['releaseStatus']] if cue else []):
        assert (status, address) not in commands
        commands.add((status, address))
    for status, addr in [(fs, address), (fs, presence)]:
        assert (status, addr) not in feedback
        feedback.add((status, addr))
    assert [e['data'] for e in c['examples']] == [0, 64, 127]
    for e in c['examples']:
        shape(e, 'data commandBytes decodedValue expectedHostValue feedbackBytes result')
        v, n = e['data'], decode(curve, e['data'])
        assert e['commandBytes'] == [cs, address, v]
        assert e['decodedValue'] == n
        host_value = (10**(n-1)-0.1)/0.9 if key == 'volume' and v else 2*n-1 if key == 'crossfader' else n
        assert math.isclose(e['expectedHostValue'], host_value, abs_tol=1e-12)
        assert e['feedbackBytes'] == [fs, address, encode(curve, n)]
        vector_count += 1
assert len(commands) == 11 and len(feedback) == 18 and commands.isdisjoint(feedback)
print('PASS shape/bounds: 9 instances; sync and cue extension gates retained')
print('PASS addresses: 11 command, 9 value feedback, 9 presence; no collisions')
print(f'PASS vectors: {vector_count} zero/mid/max examples')
for curve in ('volume-travel-7bit', 'crossfader-centered-7bit'):
    values = [decode(curve, v) for v in range(128)]
    assert values[0] == 0 and values[-1] == 1
    assert all(a < b for a, b in zip(values, values[1:]))
    assert all(encode(curve, n) == v for v, n in enumerate(values))
    max_error = max(abs(n/32768 - decode(curve, encode(curve, n/32768))) for n in range(32769))
    assert max_error <= curves[curve]['normalizedTolerance'] + 1e-12
    print(f'PASS {curve}: 128 round trips, 32769 quantization inputs; max error {max_error:.12f}')
for deck in (1, 2):
    on, off = 0x90+deck-1, 0x80+deck-1
    for status in (on, off):
        for v in range(128):
            release = status == off or v == 0
            assert release == (status != on or decode('note-gate', v) == 0)
print('PASS note edges: 512 packets; all Note Off velocities and zero Note On release')
print('BOUNDARY: static specification only; no host/port/UI/audio/lease proof')
```

Executed the embedded snippet with `python3` from the repository root; exit 0:

```text
PASS shape/bounds: 9 instances; sync and cue extension gates retained
PASS addresses: 11 command, 9 value feedback, 9 presence; no collisions
PASS vectors: 27 zero/mid/max examples
PASS volume-travel-7bit: 128 round trips, 32769 quantization inputs; max error 0.003937007874
PASS crossfader-centered-7bit: 128 round trips, 32769 quantization inputs; max error 0.003968253968
PASS note edges: 512 packets; all Note Off velocities and zero Note On release
BOUNDARY: static specification only; no host/port/UI/audio/lease proof
```

`git diff --check -- docs/ai-dj/work/contracts/conventional.md ai-dj/hosts/mixxx/conventional-profile.json docs/ai-dj/work/evidence/F09.md` returned exit 0 with no output. These are initially untracked files, so an explicit trailing-whitespace/final-newline check was also run on all three files; all passed. No package code changed; package tests do not establish additional evidence for these data/doc-only edits.

## Required integration work

Proposed coordinator-owned children, neither materialized by this worker:

1. **INTEGRATE-F09-SYNC-CONTRACT** — target 25 minutes / maximum 30; prerequisites accepted F06/F07/F09 and final F08 handoff available. Exact ownership: `ai-dj/core/actions.ts`, `ai-dj/core/capabilities.ts`, `docs/ai-dj/work/contracts/actions.md`, `docs/ai-dj/work/contracts/state.md`, `docs/ai-dj/work/evidence/INTEGRATE-F09-SYNC-CONTRACT.md`. Define only `deck.set_sync_enabled` with exact desired-state boolean arguments, affected participant/resource ownership and binding rules, current-state outcomes and explicit guard requirements. Preserve the closed union and all existing semantics. Specify fresh known loaded target/participants, passthrough/vinyl/quantize/BPM/rate conditions without arbitrary host names. Enabling may affect other participants. Address already-enabled/disabled, rejected enable, rapid on/off one-shot effects, unknown/stale state, manual takeover and cancellation without blind compensating writes. If exact guards need new host telemetry, declare those unavailable until a narrow host operation exists. Checks: `npm --prefix ai-dj run typecheck`, `npm --prefix ai-dj run build`, `git diff --check`, and a source-backed semantic review of mandatory resources/conditions. Do not update the profile gate or self-accept.
2. **INTEGRATE-F09-SYNC-VALIDATION** — target 25 minutes / maximum 30; prerequisites accepted F08 and INTEGRATE-F09-SYNC-CONTRACT. Exact ownership: `ai-dj/core/validate.ts`, `ai-dj/test/core/validate.test.ts`, `docs/ai-dj/work/evidence/INTEGRATE-F09-SYNC-VALIDATION.md`. Add validator support for only the accepted operation/resources/conditions. Check both booleans and already-satisfied intent shapes, extra fields/nonbooleans, missing or mismatched participant authority/binding, malformed new guards and rejection of unrelated unknown operations. Preserve semantic/executor separation: a well-shaped envelope does not prove guards true. Checks: `npm --prefix ai-dj run typecheck`, `npm --prefix ai-dj test`, `npm --prefix ai-dj run build`, `git diff --check`. Coordinator reviews and accepts independently.

After both are accepted, reserve a small follow-up for `ai-dj/hosts/mixxx/conventional-profile.json`, `docs/ai-dj/work/contracts/conventional.md` and its evidence to bind the new semantic selector. Until then this profile's sync gate stays intact; no address/meaning changes silently. If either child cannot finish its bounded changes and checks by minute 25, split before the deadline. These are proposed scopes, not permission to touch another live worker's files.

M03 (`ai-dj/midi/conventional-encode.ts`, `ai-dj/test/midi/conventional-encode.test.ts`) must include this profile and retain cue/sync extension gates; add both accepted integration children as entry requirements before completing semantic encoding of all five controls. M08 (`ai-dj/hosts/mixxx/fragments/sync.js`, `ai-dj/test/host/sync.test.ts`) can implement the fixed raw host setter after M05, while the semantic adapter still rejects unsupported execution. Its entry packet must require this exact profile and distinguish raw fragment tests from semantic sync completion, which requires both integration children. Any live check requires F11's exclusive runtime lease and source/profile match. M07 separately owns cue press/release and shutdown-release harness tests; production cue proof still requires the later host guard/watchdog implementation and F06 lease reconciliation. Neither depends on pretending a MIDI packet has an action ID. Coordinator must materialize the exact child/check paths and entry requirements in the ledger/catalog; this worker does not edit those shared files.

No dynamic children were materialized, no jobs were launched, and no runtime leases were held. Remaining runtime, mapping, clock/freshness, actor attribution, sync semantic integration and momentary lease proof are explicitly outside this completed specification work and remain for coordinator acceptance/routing.

> End of autonomously AI-generated evidence.
