# F06 evidence: semantic action contracts

> This document was autonomously generated by an AI agent at the user's request.

Status: authored and handed off for coordinator review; not self-accepted. Input integrated revision: `4b4fc980ed7ab4c7c985114a0e165af7f5e23969`. F02 was accepted before dispatch. The coordinator owns acceptance and the integrated F03 compiler check.

## Owned deliverables

- [actions.ts](../../../../ai-dj/core/actions.ts): data-only versioned action envelope, identities and generations, monotonic timing, bounded constants, host/authority preconditions, a closed operation union, paired momentary cleanup obligations and distinct outcome evidence.
- [actions.md](../contracts/actions.md): normative bounds and runtime-validation requirements, operation meanings, retry/cancellation/mode rules, observation/actor evidence boundaries, and play/cue/load/relative-jog walkthroughs.
- This evidence file. No package, lockfile, scaffold test, runtime, execution ledger or other worker file was edited by F06. No Git commit/push or upstream communication was performed by F06.

## Validation performed

On 2026-09-13, from the project root:

1. `node --check ai-dj/core/actions.ts` exited 0. This proves Node syntax admission only, not TypeScript type correctness or runtime behavior.
2. A bounded Python check resolved every local Markdown link in the contract, checked document/source opening and closing AI disclaimers, checked the source has no imports/exported functions, and checked both files for trailing whitespace. It exited 0 with `F06 local documentation links, disclaimers, import/function boundary and whitespace checks passed`.
3. Manual contract walkthroughs: play is an absolute boolean rather than a toggle; sent/accepted cannot complete it. Cue trigger cannot blind retry, and cue press has a separately bound release obligation. Load distinguishes old deck state from intended stable track/occurrence and requires fresh identity plus new track generation; a replacement cancels old work. Jog is one bounded signed beat delta, not a fader position, and uncertain emission cannot replay it.
4. Manual validation-boundary review: the closed union and exact-key normative schema allow F08 to reject unknown action/kind/parameter pairs and extra executable data. Every numeric role has explicit finite/integer/range rules. Reviewed invalid examples include unknown action, wrong kind, `NaN`, infinity, zero/out-of-range jog, out-of-range absolute values, mixed clock IDs, stale host/track/ownership generations, mismatched press leases and missing operation-required ownership. No runtime validator exists in F06, so these rejection behaviors are specified rather than falsely reported tested.
5. Reviewed three-mode authority: performance authority excludes PlaylistOnly; B2B requires fresh authoritative grants after release, default 3-second quiet interval and a later real transition start; explicit disarm and B2B manual-stop remain latched. Cleanup is a separate lease-only authority that cannot revive cancelled performance or release a newer press/track. AIOnly can recover ordinary manual changes through new guarded work. No authority mechanism is implemented here.

Coordinator integration: after both workers froze their files, `npm run typecheck` and `npm run build` passed on the combined scaffold and action contract. Default and focused smoke tests each passed once after the F03 selector correction. These checks prove compile compatibility, not runtime validation. No independent check is claimed while F03 owns mutable compiler/package inputs. Runtime host, MIDI, native UI, physical-controller, audible-playback, timing, endurance and listening results are not part of this contract-only evidence.

## Graph/source evidence and handoff

Parent-supplied Verify context: graph project `mixxx-ai-dj`, ready generation `2026-09-13T16:44:03Z`, 47,976 nodes and 238,220 edges. Parent `File` search for `ai-dj/*` returned zero with no further page. Coverage reported the new action/contract paths missing and `ai-dj` scope unavailable. F06 therefore read its new source directly and makes no indexed structural or exhaustive codebase claim. Documentation sources read directly: GOALS.md, F02 layout contract, F06 card, relevant PLAN/DEFAULTS sections and worker dispatch policy.

F07 must integrate actual state/capability schemas with these bounded references. F08 implements independent untrusted-input validation and adversarial rejection checks. Later execution/host tasks implement real ownership grants, cancellation, replay protection, lease-scoped release/watchdog and postcondition observation. A host profile without exact semantic support remains unsupported; this closed initial union does not remove any of the required 22 feature families. Those future tasks are existing catalog scope, not an unrecorded F06 implementation or a claim of runtime completion. No continuation or long job was started by F06.

> End of autonomously AI-generated document.
