# F12: evidence capture conventions

> This document was autonomously generated by an AI agent at the user's request.

Status: handed off for coordinator review; this worker does not accept its own task. Work was prepared against integrated source revision `0ef0499db28c6c86bb7c75e390665da831f8c20c` on 2026-09-13.

## Delivered scope

| Path | Result |
| --- | --- |
| `ai-dj/diagnostics/events.ts` | Data-only structured event types, clock-domain records, privacy-redacted raw-artifact manifests, and a bounded in-memory output buffer. It imports type aliases from `core/actions.ts` only. |
| `ai-dj/test/diagnostics/events.test.ts` | Five focused tests for event ordering/source distinction, overflow-gap reporting, copied/frozen event isolation, invalid input rejection, and artifact redaction. |
| `docs/ai-dj/work/evidence-format.md` | Required run-header, clock, ordering, artifact, redaction, bounded-capture, and evidence-class conventions. |

`command-sent`, `host-observed`, `ui-observed`, and `audio-observed` are separate discriminated event types. The definitions deliberately do not turn a send into acceptance, a host observation into native UI proof, or audio capture into a causal or listening-quality claim. Raw events copy byte arrays before buffering, and captured events are recursively frozen. Admission accepts only exact header/payload keys, bounded UTF-8 IDs and arrays, valid MIDI-byte values, supported discriminants, and finite numeric fields.

The buffer assigns capture sequence by arrival order and requires nondecreasing local capture time within its one local clock domain. It preserves an earlier valid event time/source timestamp when the event arrives later. It validates and clones before changing counters, sequence, clock, or queue state. Overflow drops the new event and emits a `CaptureGapReport` with first/last capture sequence, count, and contiguous flag. It has no filesystem, native MIDI, UI, planner, host-adapter, session-journal, or network dependency.

## Validation

The following ran from `ai-dj/` with Node `v26.8.1` and npm `11.19.0`.

| Command | Result |
| --- | --- |
| `/Users/faizkhalid/.local/bin/npm test -- test/diagnostics/events.test.ts` | Exit 0. Five focused tests passed. |
| `/Users/faizkhalid/.local/bin/npm run typecheck` | Exit 0. Integrated `tsc --noEmit` passed after F08 stabilized. |
| `/Users/faizkhalid/.local/bin/npm run build` | Exit 0. Integrated `tsc` build passed. |

The focused test includes regression checks for unknown header/payload fields, functions that would fail structured cloning, invalid and oversized MIDI bytes, overlong IDs, state equality after rejected admission, and later arrival of an earlier observed event timestamp. F12's focused test does not exercise real MIDI, Mixxx, host feedback, native UI, audio capture, controller input, timing calibration, journal persistence, or listening quality.

## Handoff

Coordinator review should confirm the exact owned paths and decide acceptance. Follow-on diagnostics/session cards should consume the event types without reinterpreting their evidence boundaries.

## Coordinator acceptance checks

Coordinator review additionally rejects accessor/hidden fields before reading event payloads, enforces F06 opaque-ID syntax and array bounds, and validates redacted manifest identity/version/type fields. Rejected events leave queue, counters and timestamps unchanged. Updated integrated typecheck/build and all 18 tests passed, including six diagnostics tests. Earlier worker counts above describe their check time; this is the integrated acceptance check.

> End of autonomously AI-generated document.
