# I13 — Stable selection and playlist inventory, page 1

> Autonomously AI-generated source and native-menu evidence. This first page does not close the family or prove executable MIDI capabilities.

[Page 1](../inventory/catalog-selection/page-01.json) records ten proposed canonical candidates: playlist browse, name lookup, name-to-ID resolution, ordered entries, distinct membership, existence, row count, lock state, hidden category, and creation. The exact source ranges, file hashes, native binary hash and context are in [the source manifest](../inventory/catalog-selection/source-manifest-01.json). Source revision is `7adf82c`; the inspected isolated native application reports 2.7.0. Source operation equivalence with that binary remains untested.

## Findings that affect the implementation

- `PlaylistDAO::getTrackIds` at lines 134–152 uses DISTINCT and no ordering. It collapses A/B/A to membership and must not feed the occurrence reader. It is a different candidate, not an alias.
- `getTrackIdsInPlaylistOrder` at lines 154–172 selects every track row ordered by position, preserving duplicates. Its public result has no occurrence-row ID, source revision, pagination or explicit error discriminator; query failure and empty playlist both return an empty list. This source primitive alone cannot satisfy P01/P02.
- `getPlaylists` returns IDs/names ordered lexicographically and filtered by category, but failure also returns an empty list. Mutable name resolution is not stable playlist identity.
- `playlistExists` and `isPlaylistLocked` conflate false with errors/missing rows. Those booleans cannot become conclusive unavailable/unlocked observations without a stronger service result. `tracksInPlaylist` counts rows, including repeats, and uses -1 for failure; separate queries still do not establish one coherent snapshot.
- The native Library menu exposed Rescan Library, Export Library to Engine DJ, two search actions, Create New Playlist and Create New Crate. Create New Playlist opened a name dialog with Cancel/Create. It was inspected and cancelled without saving; no playlist was created, no track loaded and no MIDI command sent. The host was quit after inspection.

All ten rows start with unknown availability and untested command, feedback, native UI and runtime assessments. The located creation dialog is a UI discovery location, not a successful create operation. Singleton assignment denotes a provisional host-library service endpoint; library/playlist IDs are prospective operation arguments, and their runtime enumeration and final target semantics remain open. No physical controller support is inferred.

## Scope and source verification

Graph project `mixxx-ai-dj`, generation `2026-09-13T16:44:03Z`: all 64 matching PlaylistDAO entries were read in two pages (60+4). Exact ordered-reader source was obtained and checked directly. Its inbound `EnginePrimeExportJob::loadPlaylist` use was confirmed at export source line 600. Some outbound graph matches were unrelated same-name Qt symbols and were discarded rather than treated as call evidence.

Coverage reported partial DAO implementation ranges 28, 37, 344, 1045, 1067, 1193; header 19/136; export line 806. Every reported range was read directly, along with the chosen method bodies and declarations. This bounded check does not prove source-family completeness.

JSON Schema validation passed for all ten rows with explicit separate assessment dimensions and continuation. Additional review checked unique candidate/evidence IDs, exact shared context, readable source ranges/hashes, non-aliased membership versus ordered-read semantics, existing screenshot references and no passed behavioral assessments. No application code changed and no live database queries/writes were performed.

## Continuation

I13 remains split/open. `I13-PAGE-02` owns the next immutable page, starting at cursor `I13-page-02`, with previous page `I13-page-01`. Remaining work includes mutation/reorder/shuffle semantics and aliases, stable selected-track identity, sidebar/table context menus, matching-version manual, runtime target enumeration and any further bounded pages. The known macOS accessibility selection crash remains an independent release issue; this menu/dialog inspection did not reproduce or fix it.

The family closure and P02 service specification stay pending until required continuation evidence is accepted. No unknown scope is silently removed from the denominator.

> End of autonomously AI-generated discovery evidence.
