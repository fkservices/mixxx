# M04 evidence: conventional feedback decoder

> This document was autonomously generated by an AI agent at the user's request.

Status: ready for coordinator review; this worker does not self-accept. Input integrated revision: `0eeae4447ca1f6a1f2e3af683ee8061a85888481`. Dependencies F08 and F09 were accepted. This task changed only [conventional-decode.ts](../../../../ai-dj/midi/conventional-decode.ts), [conventional-decode.test.ts](../../../../ai-dj/test/midi/conventional-decode.test.ts), and this evidence file.

## Result

`decodeConventionalFeedback` decodes the nine frozen instances in the F09 profile: play, volume, cue signal, sync enabled for decks 1 and 2, plus mixer crossfader. It accepts only exact three-byte feedback CC status/address pairs and their valid profile values. Boolean feedback accepts only `0`/`127`; presence accepts only `0` unavailable, `64` unknown, or `127` present; volume uses `v / 127`; crossfader preserves byte 64 as normalized `0.5`.

Each accepted packet carries a validated local monotonic receipt time and explicit `{ actor: "unknown", correlation: "none", hostObservation: "unproven" }` provenance. Output is a frozen copy. Cue feedback is labeled `control-signal-only`; it is not a lease, active hold, release result, seek result, request, acknowledgement, action ID, track identity, generation, or authoritative F07 observation. Node receipt time is deliberately not promoted to host-observation time.

Malformed, unrelated, system realtime, noncanonical boolean/presence, and all Note On/Note Off cue command/release forms return `null`. The module opens no native MIDI ports, sends no commands, holds no mutable state, and has no host/UI/process/timer dependency.

## Checks

Run from `ai-dj/`:

| Command | Result |
| --- | --- |
| `node --test --experimental-strip-types test/midi/conventional-decode.test.ts` | Exit 0; 5 tests passed |
| `npm run typecheck` | Exit 0 |
| `npm run build` | Exit 0 |

The tests read the checked-in F09 profile in test code only and lock all nine IDs/capabilities/decks/meanings, every published zero/mid/max feedback vector, and every presence route/value. They also cover both cue channels' Note On zero and Note Off values `0`, `64`, and `127`, plus wrong packet lengths, bad data/address/status, system clock, receipt-time bounds, frozen output/packet/provenance copies, and absence of inferred request/action/lease fields.

## Limits and remaining work

This is deterministic profile decoding, not actual Mixxx feedback, endpoint pairing, presence establishment, callback freshness, reordered-packet reconciliation, F07 promotion, native UI evidence, audio behavior, or cue lease handling. Later integration must bind a known endpoint/session/profile and independent identity/freshness evidence before raw packets can affect authoritative observed state. M07/M08 own cue/sync mapping behavior and F11 owns isolated runtime evidence.

> End of autonomously AI-generated document.
