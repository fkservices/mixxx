# F01 evidence — freeze the local test profile

> This document was autonomously generated by an AI agent at the user's request.

## Handoff

| Field | Value |
| --- | --- |
| Card | F01 |
| Worker start | `2026-09-13T18:40:15.808Z` |
| Worker end / evidence captured through | `2026-09-13T18:44:15Z` |
| Source revision at start and final check | `4067eb8e1990f5d6fec991f6d4b2a9b191a51abe` |
| Source branch | `codex/ai-dj-plan` |
| Paths changed | `docs/ai-dj/work/baseline.md`; `docs/ai-dj/work/evidence/F01.md` |
| Runtime lease | None |
| Installation/build/job handles | None |

F01 completed its documentation scope. It selected Mixxx 2.5.6 ARM with LateNight/PaleMoon and an exact disposable profile, but it did not prove a runtime. Mixxx and the two operator-approved fixture tracks are missing prerequisites for F11.

## Commands and results

Revision and checkout:

```text
$ git rev-parse HEAD
4067eb8e1990f5d6fec991f6d4b2a9b191a51abe
$ git branch --show-current
codex/ai-dj-plan
```

Host:

```text
$ sw_vers
ProductName:            macOS
ProductVersion:         27.0
BuildVersion:           26A5416b
$ uname -m
arm64
$ sysctl -n hw.model
MacBookPro18,3
$ sysctl -n hw.memsize
34359738368
$ sysctl -n hw.logicalcpu
8
$ sysctl -n hw.physicalcpu
8
```

The serial number was deliberately not queried. A filtered `system_profiler SPHardwareDataType` read reported `Apple M1 Pro`, 8 cores (6 performance and 2 efficiency), and 32 GB memory.

Tool availability:

```text
node    /Users/faizkhalid/.local/bin/node    v26.8.1
npm     /Users/faizkhalid/.local/bin/npm     11.19.0
cmake   NOT_ON_PATH
ninja   NOT_ON_PATH
clang   /usr/bin/clang                       Apple clang version 17.0.0 (clang-1700.6.3.2)
brew    /opt/homebrew/bin/brew               Homebrew 6.0.21
python3 /usr/local/bin/python3               Python 3.10.7
git     /opt/homebrew/bin/git                git version 2.52.0
```

Installed Mixxx check:

```text
$ test -d /Applications/Mixxx.app
not found
$ test -d /Users/faizkhalid/Applications/Mixxx.app
not found
$ command -v mixxx
<no output>
$ brew list --cask mixxx
find: /opt/homebrew/Caskroom/mixxx: No such file or directory
```

No install command was run. No `Info.plist` existed at either expected application location, so there is no installed-version read-back to report.

Profile isolation check:

```text
$ ls -ld "/Users/faizkhalid/Library/Application Support/Mixxx"
No such file or directory
$ ls -ld "/Users/faizkhalid/Library/Application Support/Mixxx-AI-DJ/profiles/mixxx-2.5.6-latenight-f01"
No such file or directory
```

The checkout's `src/util/cmdlineargs.cpp` defines `--settings-path` as the top-level settings directory. The selected path is therefore explicit and does not depend on changing the normal Mixxx profile. It was not created in F01.

Audio/MIDI snapshot:

```text
$ system_profiler SPAudioDataType
MacBook Pro Microphone: default input, 1 channel, 48000 Hz
MacBook Pro Speakers: default output/system output, 2 channels, 48000 Hz
$ system_profiler SPMIDIDataType
<no device records returned>
```

Only the default built-in devices are retained in this evidence. Mixxx audio settings, buffer size, IAC routes, and controller presence were not observed.

Source/runtime distinction:

```text
$ sed -n '510,513p' CMakeLists.txt
project(mixxx VERSION 2.7.0 LANGUAGES C CXX)
# Work around missing version suffixes support https://gitlab.kitware.com/cmake/cmake/-/issues/16716
set(MIXXX_VERSION_PRERELEASE "alpha") # set to "alpha" "beta" or ""
```

This is source metadata for the inspected checkout, not an available executable. The selected first executable is the official stable 2.5.6 macOS ARM build. The [official Mixxx download page](https://mixxx.org/download/) reported 2.5.6 stable and recommended it for live use at inspection time; the [2.5 user-interface manual](https://manual.mixxx.org/2.5/en/chapters/user_interface) documented the LateNight/PaleMoon legacy skin. No artifact was downloaded and no published checksum was copied without verifying the actual future download.

Fixture check:

```text
$ find . -maxdepth 4 -type f | <audio-extension filter>
./src/test/stems/02-bass.wav
./src/test/stems/03-melody.wav
./src/test/stems/01-drum.wav
./src/test/stems/mainmix.wav
./src/test/stems/04-vocal.wav
./src/test/sine-30.wav
... tag-parser fixtures under ./src/test/id3-test-data/
```

Those files were rejected as musical/listening fixtures. The dispatch included no operator-approved song identities or paths, and F01 did not adopt files from a private music library. `fixture-track-a` and `fixture-track-b` remain explicit operator inputs.

## Validation against acceptance

| Acceptance item | Result |
| --- | --- |
| Precise profile; observed, selected, and missing separated | Pass in `docs/ai-dj/work/baseline.md` |
| Installed host metadata verified or installation pending | Pending honestly: no Mixxx app/executable found; proposed `SETUP-F01-MIXXX-2.5.6-ARM` blocks F11 |
| Two safe fixture tracks identified | Pending honestly: proposed `SETUP-F01-FIXTURE-TRACKS` blocks F11; no guessed filenames |
| Production profile protected | Pass: normal profile was absent and untouched; selected `--settings-path` is separate and was not created |
| Same-machine desktop scope preserved | Pass |
| Timing/capacity targets and measurement recipe frozen | Pass as selected targets only; zero measurements claimed |
| No install, launch, controller/profile change, or runtime implementation | Pass |
| Next card | F02 becomes eligible after coordinator accepts F01; F11 remains blocked on setup inputs |

## Limitations and next actions

- Official web documentation supports the selected version and skin, but local executable metadata is unavailable until the proposed install job finishes.
- Node `v26.8.1` is present but its native MIDI binding compatibility is untested; F04/F05 owns that proof or an explicit version correction.
- Current audio enumeration is not proof that Mixxx can open the selected route with the eventual buffer size. F11 owns that validation.
- No physical controller identity was supplied. O04/R23 owns the later real-controller prerequisite; F11 may prepare the controller-free disposable fixture once its own prerequisites are met.
- No timing, host-state, UI, audio-onset, capacity, or musical-quality result was produced in F01.

> End of autonomously AI-generated document.
