# M01: MIDI transport port discovery

> This document was autonomously generated by an AI agent at the user's request.

Status: handed off for coordinator review; this worker does not accept its own task. Work was prepared against integrated source revision `e058660bbf2a1612ab7bd8ba76cf8e1d65ff865b` on 2026-09-13.

## Delivered scope

| Path | Result |
| --- | --- |
| `ai-dj/midi/ports.ts` | A narrow enumeration-handle interface, native-binding adapter, read-only inventory, exact endpoint resolver, and cleanup handling. It exposes no open, virtual-port, send, stream, or callback operation. |
| `ai-dj/test/midi/ports.test.ts` | Fake-backed tests for no-port inventory, exact-name missing/ambiguity rejection, reordered indexes through fresh enumeration, binding exceptions, and cleanup. |

The resolver accepts an exact command-output name and feedback-input name. Each resolve operation obtains a new inventory, finds exactly one endpoint per requested direction, and returns the observed index only for that inventory. It does not treat an older index as a stable endpoint identity. Missing or duplicated names reject with a typed `EndpointSelectionError`.

The native import is confined to `createNativeMidiPortEnumerator`. Enumeration constructs only the binding's `Input` and `Output` handles, reads counts/names, and destroys both handles in a `finally` path. M02 owns any open, virtual-port, filter, listener, close, or send lifecycle.

## Real local enumeration

The following read-only command ran once from `ai-dj/` under the selected Node binary. It imported `@julusian/midi`, constructed `Input` and `Output`, read their counts/names and unopened state, then called `destroy()` on both in `finally`. It did not call `openPort`, `openVirtualPort`, `sendMessage`, or a stream helper.

```sh
/Users/faizkhalid/.local/bin/node --input-type=module --eval 'import midi from "@julusian/midi";
const input = new midi.Input();
const output = new midi.Output();
let record;
try {
  const inputCount = input.getPortCount();
  const outputCount = output.getPortCount();
  record = {
    input: { count: inputCount, names: Array.from({ length: inputCount }, (_, index) => input.getPortName(index)), isPortOpen: input.isPortOpen() },
    output: { count: outputCount, names: Array.from({ length: outputCount }, (_, index) => output.getPortName(index)), isPortOpen: output.isPortOpen() }
  };
} finally {
  input.destroy();
  output.destroy();
}
console.log(JSON.stringify({ node: process.version, record, cleanup: "input-and-output-destroyed" }));'
```

Result: exit 0 on Node `v26.8.1`.

```json
{
  "input": { "count": 0, "names": [], "isPortOpen": false },
  "output": { "count": 0, "names": [], "isPortOpen": false },
  "cleanup": "input-and-output-destroyed"
}
```

There were no local endpoint names to select. This is current inventory evidence, not a binding failure or a claim that future Hercules, IAC, or virtual endpoints cannot appear. The coordinator was notified immediately after cleanup, so F11 can begin its separate setup without this M01 check holding the MIDI-enumeration lease.

## Validation

All commands ran from `ai-dj/` with Node `v26.8.1` and npm `11.19.0`.

| Command | Result |
| --- | --- |
| `/Users/faizkhalid/.local/bin/npm test -- test/midi/ports.test.ts` | Exit 0. Four focused tests passed. |
| `/Users/faizkhalid/.local/bin/npm run typecheck` | Exit 0. Integrated strict typecheck passed. |
| `/Users/faizkhalid/.local/bin/npm run build` | Exit 0. Integrated TypeScript build passed. |

These checks do not prove physical controller discovery, virtual-port creation, Mixxx pairing, MIDI routing, feedback, audio, UI, or timing behavior.

## Handoff

Coordinator review should confirm the exact owned paths and decide acceptance. M02 must re-enumerate immediately before it opens a selected endpoint and fail closed if names are missing, duplicated, or changed.

> End of autonomously AI-generated document.
