# R02 lifecycle acceptance

> Autonomously AI-generated state-machine review; no runtime implementation claim.

Reviewed from revision `8abda83` using accepted R01 and F07, with F06 outcomes and authority rules as constraints. [Lifecycle v1](../contracts/lifecycle.md) separates connection readiness from armed state; freezes negotiation/challenge/session boundaries, wire versus action/state sequencing, bounded deduplication, capability and snapshot transactions, independent outcome evidence, epoch invalidation, cancellation, heartbeat loss and clock uncertainty.

## Required walkthrough review

| Race | Required result |
| --- | --- |
| Host changes volume after snapshot freeze | Buffer the contiguous delta, atomically install the frozen baseline, then apply the delta; missing predecessor produces a gap rather than fabricated state |
| Manual load precedes a late acceptance/result | Advance track generation and cancel old grants; retain the late event in old-action history; no old-track restoration or new-binding completion |
| Setter is already satisfied | With fresh state and current guards, record state-only satisfaction and send nothing; no fabricated receipt or refreshed timestamp |
| Client disappears after cue press | Independent host watchdog retires the exact lease; unresolved cleanup stays explicit; no generic release against a later press |

The review also checked that recovery cannot rearm, conventional MIDI cannot imply request correlation or actor identity, and heartbeats do not refresh control observations. Scope-specific B2B holds require both a later transition and three seconds of quiet; manual transport holds remain latched. PlaylistOnly cannot load or mix. Already-sent effects remain observable after cancellation, with no compensating sync write.

Snapshot phases use exact context and immutable totals plus SHA-256 of length-prefixed original items-page bytes. Cross-page validation occurs before atomic publication. Stream sequences/revisions are distinct from wire sequences so bulk-transfer gaps cannot stall disarm handling. Aggregate storage/node/record/page limits and absolute/idle timeouts are explicit. Current F08 per-message validation does not prove aggregate consistency; the required R08-PAGED-VALIDATION child records that remaining implementation work.

The contract names implementation obligations under R06–R12 and later mode/ownership work. It does not claim closed message validators, host atomicity, entropy/clock providers, native heartbeat or real recovery have been implemented. Missing host support remains visible as required extension work.

Markdown lint, generated-document checks and the project planning validator passed. R02 is accepted as lifecycle semantics and reviewed races only; native, latency, physical-controller and musical gates remain open.

> End of autonomously AI-generated evidence.
