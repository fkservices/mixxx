# M03: conventional command encoder evidence

> Autonomously AI-generated implementation evidence at the user's request.

Input revision `972717d`; F08, F09 and all three sync integration children accepted. Coordinator implementation and review; no worker dispatch or runtime lease transfer.

[Encoder](../../../../ai-dj/midi/conventional-encode.ts) implements the frozen nine-instance wire projection. `encodeConventionalCommand` validates the complete action envelope, checks an explicit pair of distinct opaque deck identities, and encodes supported play/volume/crossfader requests. Output packets are frozen and explicitly marked authorization unproven. No host identity, current state, arming, ownership or timing truth is inferred. Unknown deck identities, unsupported actions and malformed input return no packet.

Sync and cue semantic requests remain extension-needed with their exact host guard IDs, including valid cue cleanup and both sync booleans. The separately named `encodeConventionalWire` serializes raw fixture/adapter values for all five families, including canonical Note Off and explicit Note On zero cue releases. It does not validate semantic intent, authorize sending, pair leases, schedule release, retry or access a MIDI port. Its raw packets are not an executor bypass: every production send still requires independent admission and host reconciliation.

## Verification

[Seven tests](../../../../ai-dj/test/midi/conventional-encode.test.ts) check all 27 profile vectors, both deck channels, action schema versions and extension gates, canonical booleans, all 128 quantized values for both volume routes and centered crossfader, cue edge alternatives, immutable packets, explicit opaque routing, malformed/duplicate/accessor routes, unsupported controls, nonfinite/range rejection, semantic volume/play/crossfader encoding, valid cue press/release rejection and valid sync true/false rejection pending host guards.

Using the local Node 26 toolchain, from `ai-dj/`:

| Check | Result |
| --- | --- |
| `npm test` | 64 passed, including seven encoder cases |
| `npm run typecheck` | Passed |
| `npm run build` | Passed |

Graph coverage for this new encoder was missing and the existing decoder/validator was untracked in generation `2026-09-13T16:44:03Z`; direct source and frozen profile reads supplied the bounded evidence. Runtime, actual Mixxx mapping, native UI changes, latency, audio, controller handover and musical behavior are not tested by this pure module. M05–M10 and later executor/host-operation tasks remain required.

> End of autonomously AI-generated implementation evidence.
