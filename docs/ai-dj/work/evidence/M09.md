# M09: conventional feedback subscriptions and refresh

> Autonomously AI-generated implementation evidence at the user's request.

Input revision `f29473a`; M05 accepted. Coordinator implementation; no runtime lease or live mapping change.

[Feedback fragment](../../../../ai-dj/hosts/mixxx/fragments/conventional-feedback.js) subscribes to the nine fixed controls. Startup emits presence before each valid sampled value. Change callbacks re-read the exact profile API, using parameter travel for volume/crossfader rather than a raw callback payload. One 100 ms timer refreshes all controls, including unchanged state. Only B8/B9/BA feedback/presence routes are emitted; the input handler never responds to commands.

A successful connection plus a valid sample permits present/value packets. Failed subscriptions, disconnected handles, invalid samples and read exceptions emit unknown presence (64) and suppress the value. They never fabricate false/zero or claim confirmed absence. This fragment has no independent absence evidence, so it does not emit unavailable (0). Boolean values are strictly 0/1 before conversion to 0/127; parameter values must be finite in [0,1]. Crossfader center remains exactly 64.

Shutdown disables callbacks and disposes all handles through the bootstrap. A lifecycle generation also rejects queued subscription/timer callbacks after subsequent reinitialization. Partial initialization failure disposes acquired subscriptions. MIDI output errors are not treated as delivery success.

[Five tests](../../../../ai-dj/test/host/conventional-feedback.test.ts) cover all nine initial samples and presence ordering; the configured 100 ms interval; all 27 profile vectors; raw callback versus normalized parameter values; missing/disconnected/invalid/read-failed controls; periodic state changes without callbacks; no command echo; shutdown cleanup; old callbacks before and after reinit; and initial output failure cleanup.

| Local check | Result |
| --- | --- |
| `npm test` | 90 passed |
| `npm run typecheck` | Passed |
| `npm run build` | Passed |

New-file graph coverage was missing in generation `2026-09-13T16:44:03Z`; direct F09 profile/contract and harness source supplied bounded evidence. No exhaustive graph claim was made.

The timer interval is configured, not measured. Receiver-side 500 ms expiry, stream/context identity, host timestamps, loss/disarm behavior and reordered-packet reconciliation remain later integration work. Cue feedback is still only its input-control signal. Sync boolean feedback supplies no leader, phase, tempo, queue or semantic guard proof. Actor and request correlation remain unknown. M10 assembles this module before native feedback/UI/audio/timing tests; the current running bootstrap is unchanged.

> End of autonomously AI-generated implementation evidence.
