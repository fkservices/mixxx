# F11: Disposable Mixxx runtime fixture evidence

> Autonomously AI-generated evidence at the user's request.

Status: fixture preparation complete; accepted by the coordinator after the observations below. Performance control, audio playback, timing distributions and musical acceptance remain separate required tasks.

## Observed native runtime

Mixxx 2.5.6 ARM launched from the verified official application. The first selected profile outside the app container failed with SQLite error 14 despite an available QSQLITE driver. The installed binary has the macOS app-sandbox entitlement. Moving the disposable profile inside its own app container resolved startup without changing entitlements or using the normal Mixxx profile. See the [runtime fixture](../runtime-fixture.md) for the corrected launch path.

LateNight / PaleMoon is selected. Core Audio routes the stereo main output to MacBook Pro Speakers, channels 1–2, at 48,000 Hz. The native audio buffer is 5.33 ms (256 frames); system-reported latency is 22.7083 ms. Underflows changed from 0 to 1 while applying the buffer change. That is a setup counter, not an uninterrupted-playback or latency-distribution result.

[Actual audio preferences screenshot](../screenshots/mixxx-audio-baseline-2026-09-13.jpg), captured 2026-09-13T19:30:12.802Z, shows the selected settings. No track paths or music content appear.

## Two-direction diagnostic proof

A private Node helper creates one virtual Output and one virtual Input, each with the logical name `AI DJ`. Mixxx logs explicitly opened input index 0 and output index 1 for that paired name. The private diagnostic mapping has no deck, mixer, or library writes and returns only one fixed response to its probe.

- Sent: `B0 77 2A` at monotonic nanoseconds `10533792977500`.
- Mixxx debug log: incoming channel 1/control 0x77/value 0x2A; diagnostic handler called; outgoing channel 9/control 0x77/value 0x2A.
- Received: `B8 77 2A` at monotonic nanoseconds `10533794293208`.
- The Node receive callback only copies bounded bytes into a queue; it never sends or echoes. Its only send entrypoint is an explicit signal limited to ten identical probes. The mapping recognizes only the command status; feedback status cannot re-enter it.

This one observed round trip proves the diagnostic route in both directions. Its 1.316 ms duration is a single sample, not a percentile, control response, native UI response, audio latency or musical timing claim. No performance commands were sent.

[Actual mapping screenshot](../screenshots/mixxx-midi-probe-2026-09-13.jpg), captured 2026-09-13T19:30:50.589Z, shows the enabled route-only mapping. Other sidebar entries are OS HID endpoints, not claims of connected DJ controllers.

Raw job/log/mapping files remain outside Git in the private F11 job and profile directories. Source revision: `e058660bbf2a1612ab7bd8ba76cf8e1d65ff865b`. Profile-only diagnostic mapping is an explicit F11 scope clarification; production mapping remains M05 onward.

## Two-track native baseline and final check

Both approved Music-library fixtures were loaded through the native File → Load Track to Deck menus. At 2026-09-13T19:32:52.416Z, the actual two-deck view displayed analyzed waveforms, fixture A at 117.00 BPM/key A and fixture B at 102.00 BPM/key B-flat. Both were paused; positions remained at the cue/start, not running playback. These are host analysis outputs, not independently verified beat grids, phrase boundaries or harmonic compatibility. This pair is suitable for isolated controls; its tempo/key difference must not be treated as an accepted natural transition under normal musical limits.

The private screenshot `jobs/f11-runtime/decks-loaded.jpg` and full `decks-loaded-ax.txt` retain the exact native display. They stay outside Git because the library pane contains unrelated local recording titles. Public gallery captures show only native audio/controller preferences. Both public JPEGs are 972 × 768 pixels, inspected for legibility and private data.

A read-only SQLite lookup matched fixtures A/B to isolated library IDs 321/364. The private `baseline.json` records source-hash checks, deck observations and stored rows. Stored analysis can lag the native display: B's stored BPM remained 0 while its native analyzed display was 102.00. No database writes were used. Both source MP3 SHA-256 hashes still matched their pre-test manifests; no source tags or user cue edits were made.

One analyzer warning trimmed 1,152 sample frames at fixture A's MP3 end (expected ending frame 11,655,936; readable ending frame 11,654,784). Loading and analysis completed. Later audio/track-end tests must use observed duration and assess this boundary; no audio-quality pass is claimed here.

After both tracks loaded, native Sound Hardware still reported 48,000 Hz, 5.33 ms buffer, 22.7083 ms system latency and underflow count 1. Two decks are visible; the host's persisted sound configuration declares four engine decks, of which this initial fixture uses only channels 1 and 2. No headphone, microphone, broadcast or recording test was performed.

A final event-log review after several minutes contained exactly ready → one probe send → one feedback receive, with no repeated feedback, echo or gap event. The correct isolated Mixxx process and both virtual endpoints remain owned by the durable fixture job for sequential handoff. The normal Mixxx profile remains absent and unused. The physical controller is still deferred by the operator.

Acceptance is limited to F11's real host setup, two usable loaded fixtures, exact paired virtual directions, diagnostic route proof, native settings and baseline counters. It does not accept production mapping, control correctness, B2B, audio or end-to-end DJ behavior.

> End of autonomously AI-generated evidence.
