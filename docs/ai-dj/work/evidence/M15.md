# M15 — Idle MIDI timing

> Autonomously AI-generated measured evidence at the user request.

The isolated native Mixxx build completed 400 loopback calibration echoes followed by 1,000 deck-one volume commands. Every sample was retained and paired; no missing feedback, capacity loss, clock fault or counter overflow was reported. The helper exited successfully after 399.12 seconds including the wait for host startup, restored volume to 127/127, and closed its ports. The native host was quit and the profile mapping restored without diagnostic activation.

| Measurement | Samples | Median ms | p95 ms | p99 ms | Maximum ms |
| --- | ---: | ---: | ---: | ---: | ---: |
| Full MIDI loopback RTT | 400 | 0.24 | 0.74 | 1.36 | 11.91 |
| Host command to matching state | 1,000 | 1.09 | 2.90 | 4.59 | 12.59 |
| Host dispatch lateness | 1,000 | -0.41 | 1.77 | 3.61 | 84.73 |

Calibration used a native echo peer in the same Node process. The host phase used the dedicated AI DJ virtual MIDI route and the custom native Mixxx build. Decks remained empty and paused. Core Audio was configured at 48 kHz with a displayed 21.3 ms buffer; the native preferences reported zero underflows and 38.7083 ms system latency after sampling. That reported latency is not an audio measurement.

The nominal cadences were 50 ms and 250 ms. Each next deadline was relative to the previous actual dispatch, with no accumulated catch-up slots. Node timers sometimes fired slightly early: host dispatch gaps ranged from 248.14 to 334.73 ms. Four dispatches exceeded the 10 ms lateness budget; the 84.73 ms maximum remains in the report. This is a bounded nominal-rate diagnostic, not an exact metronome or proof of scheduler suitability.

A single pending changed volume value was matched to the first returned MIDI value within 200 ms; calibration used unique echoed bytes and a 40 ms deadline. Value matching is not causal acknowledgement. All timing endpoints use one Node performance clock. Loopback RTT is neither halved nor subtracted from command observation. Conventional periodic feedback, the cue diagnostic stream, synchronous raw logging, and ordinary desktop/screenshot activity remained present. No synthetic load was added.

Independent verification checked all sequential sample IDs, command bytes, dispatch targets, received matching bytes, pair arithmetic, expected counts, and volume restoration. [The run record](../runs/M15.json) retains every timing endpoint, metrics, route/profile details, resource counters and hashes for private raw logs and the exact runner. Private artifacts are in the local idle-timing job directory. Native runtime and buffer screenshots are linked from the visual journal.

M15 measurement scope is accepted. Load, UI, audio, physical-controller and musical evidence remain pending; M19 must assess the dispatch outlier alongside those tests. No overall timing or delivery gate passes from this run alone.

> End of autonomously AI-generated measured evidence.
