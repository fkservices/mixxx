# M07: crossfader and raw cue mapping

> Autonomously AI-generated implementation evidence at the user's request.

Input revision `8705534`; M05 accepted. Coordinator implementation; no runtime lease, host installation or performance commands.

[Fragment](../../../../ai-dj/hosts/mixxx/fragments/fader-cue.js) implements the frozen B2/24 crossfader route with normalized parameter travel, exact byte-64 center, and 0/1 endpoints. Cue uses only `cue_preview` on Channel1/Channel2, accepting positive Note On as press and zero Note On or every Note Off velocity as release. Channel, status, address, group and data bounds must all match. Declared controls are probed using connection handles rather than zero-valued reads.

Local pending-release bookkeeping tracks attempted presses, including a setter exception after an uncertain write. Shutdown releases only locally pending cue requests, attempts each deck independently and does not reset crossfader position. Missing connections or release exceptions fail cleanup and engage the bootstrap's reinitialization refusal. Successful setter return is not proof that host preview stopped. Raw notes carry no track/press identity, so this bookkeeping cannot establish a production lease or resolve intervening human/track changes. The independent production cue host guard remains required and the semantic encoder continues rejecting cue dispatch without it.

[Seven tests](../../../../ai-dj/test/host/fader-cue.test.ts) cover all 128 crossfader bytes, all 512 cue status/velocity/deck combinations, repeated presses, both release forms, local shutdown releases, idempotent shutdown, unavailable routes, disconnected pending cues, independent cleanup after one deck throws, malformed/unrelated packets and a press setter that writes then throws.

Final local checks:

| Check | Result |
| --- | --- |
| `npm test` | 80 passed |
| `npm run typecheck` | Passed |
| `npm run build` | Passed |

These are deterministic host-stub checks. The unchanged F09 contract supplies the released cue/crossfader semantics. New-file graph coverage was missing in generation `2026-09-13T16:44:03Z`; exact source and contract reads supplied the evidence. No complete source-graph assertion is made.

M10 must assemble the fragment before native checks. Actual cue seeks, playback/stop behavior, track/press binding, disconnect watchdog, feedback, audio and timing remain later work. The running Mixxx bootstrap remains unchanged by this card.

> End of autonomously AI-generated implementation evidence.
