# M02: bidirectional virtual MIDI lifecycle

> This document was autonomously generated by an AI agent at the user's request.

Status: handed off for coordinator review; this worker does not accept its own task. Work began from integrated revision `0eeae4447ca1f6a1f2e3af683ee8061a85888481` on 2026-09-13.

## Delivered scope

| Path | Result |
| --- | --- |
| `ai-dj/midi/connection.ts` | A narrow native adapter that creates a same-name `AI DJ` pair: `Output` supplies commands to the host and `Input` receives host feedback. It imports the native binding only through `openNativeVirtualMidiConnection`. |
| `ai-dj/test/midi/connection.test.ts` | Six fake-backed lifecycle regressions for filter configuration, copied feedback, partial-open cleanup, duplicate names, send loss, cleanup failure/idempotence, option snapshots, and bounded enumeration. |

The adapter enables SysEx while filtering MIDI timing and active sensing through `input.ignoreTypes(false, true, true)`. It rejects pre-existing exact external names and inventories larger than M01's 1,024-per-direction limit before either virtual port opens. The feedback callback copies and freezes valid byte arrays; closing first removes the listener and disables the callback gate. It cleans both handles after a partial open, and a completed or failed close is at-most-once so an already-destroyed handle is never retried.

`onTransportLoss` reports startup, send, and close failures. The native binding has no host-disconnect callback here, so a silent remote host loss is not detected until a later transport/reliability card. This adapter does not plan, execute DJ actions, control audio, or use UI APIs.

## Real native lifecycle and pairing

The accepted F11 helper identity was verified from `/Users/faizkhalid/Library/Application Support/Mixxx-AI-DJ/jobs/f11-runtime/job.json`: PID `49495`, command `/Users/faizkhalid/Library/Application Support/Mixxx-AI-DJ/jobs/f11-runtime/ports.cjs`, and its recorded start. It was then terminated with `SIGTERM` before M02 opened replacement names. No duplicate F11 helper remains.

An initial one-shot private probe used the production adapter, opened both virtual directions named `AI DJ`, sent `B0 77 2A`, injected only `F0 7D 01 F7` through a separate feedback-output probe, then closed all handles. Its result was:

```json
{"status":"completed","feedback":[{"deltaTimeSeconds":0,"bytes":[240,125,1,247]}],"losses":[],"feedbackProbe":[{"index":0,"name":"AI DJ"}]}
```

After that close, fresh native `Input` and `Output` enumeration returned `{"input":[],"output":[]}`, proving the one-shot endpoints were removed. The first paired-host attempt had no fixed reply because Mixxx had not re-enumerated it.

For the required host re-pair, the bounded private keeper `/Users/faizkhalid/Library/Application Support/Mixxx-AI-DJ/jobs/m02-runtime/keeper.mjs` was launched with the current production `connection.ts`, detached with `subprocess.Popen(..., start_new_session=True)`. Its durable manifest is `/Users/faizkhalid/Library/Application Support/Mixxx-AI-DJ/jobs/m02-runtime/job.json`; it records its PID `73352`, command, source hashes, 7,200-second maximum lifetime, profile, and `SIGTERM` cleanup. The prior launchd variant was explicitly booted out because its endpoints were absent from independent enumeration; it has `KeepAlive=false`, is not loaded, and no plist remains in `~/Library/LaunchAgents`.

While the keeper held the endpoints, independent native enumeration returned exactly one endpoint in each direction:

```json
{"input":[{"index":0,"name":"AI DJ"}],"output":[{"index":0,"name":"AI DJ"}]}
```

A second real `openNativeVirtualMidiConnection` attempt used the same name and callbacks while the keeper was live. It rejected before opening any virtual port with `{"rejected":"PortNameCollisionError","direction":"input","portName":"AI DJ"}`.

The coordinator then verified that Mixxx PID `73988`, started with the same isolated F11 profile, displayed `AI DJ` in native Controllers with the `AI DJ Fixture Probe` mapping selected and enabled. Exactly one `SIGUSR1` was sent to the keeper; that trigger is restricted to the fixed command `B0 77 2A`, has no receive-to-send path, and produced the mapping's fixed feedback `B8 77 2A`:

The coordinator's native UI capture, [mixxx-midi-lifecycle-2026-09-13.jpg](../screenshots/mixxx-midi-lifecycle-2026-09-13.jpg), was saved at `2026-09-13T19:55:56.696Z` (972×768, 77,868 bytes) and shows the final paired `AI DJ` fixture mapping enabled.

```json
{"status":"ready","sent":1,"feedback":[[184,119,42]],"losses":[]}
```

The durable keeper remains under the runtime lease for later M05 work. It neither autoechoes nor re-arms its single probe.

## Validation

Commands ran from `ai-dj/` with Node `v26.8.1` and npm `11.19.0`.

| Command | Result |
| --- | --- |
| `npm test -- test/midi/connection.test.ts` | Exit 0; six focused tests passed. |
| `npm run typecheck` | Exit 0; strict `tsc --noEmit` passed. |
| `npm run build` | Exit 0; TypeScript emit passed. |

The real probe proves only virtual endpoint lifecycle, deliberate SysEx acceptance, host pairing, and the fixture command/reply bytes. It does not prove physical Hercules-controller behavior, broader MIDI semantics, action planning, audio output, listening quality, timing calibration, or detection/recovery of an unplugged or silent host transport.

## Handoff

Coordinator review should confirm the three owned repository paths and runtime manifest before accepting M02. The next holder of the runtime lease must reuse or explicitly stop the recorded keeper rather than creating a duplicate `AI DJ` pair.

> End of autonomously AI-generated document.
