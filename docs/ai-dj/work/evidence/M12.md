# M12 actual Mixxx play, volume and crossfader proof

> Autonomously AI-generated native runtime evidence at the user's request.

On 2026-09-13 the coordinator operated the installed Mixxx 2.5.6 ARM application with the isolated LateNight fixture profile, using the existing TypeScript service and separate CLI. [Structured run evidence](../runs/M12.json) records exact commands, before/after feedback, source hashes and private screenshot/capture hashes. The reviewed repository revision is `edf8c5a`; service PID 4355's loaded main/CLI hashes match current source. Installed mapping XML and assembled script hashes independently match the repository files. Host PID 23046 uses the explicit isolated settings path.

## Runtime recovery

A separate default-profile Mixxx instance was found while UI inspection targeted the wrong application instance. The coordinator cancelled its startup library chooser and quit it. The isolated direct-launch process was explicitly stopped after identity verification, and relaunched using macOS `open -n -a /Applications/Mixxx.app --args --settings-path ... --controller-debug`. Exactly one resulting Mixxx process was observed with the intended profile. The service and its virtual ports remained alive and disarmed throughout.

The AI DJ mapping was disabled. Accessibility checkbox/button actions changed its displayed state but did not persist activation; reopening preferences showed it disabled. Native coordinate clicks on the observed checkbox and Apply button activated the mapping, after which the existing service received fresh presence/value feedback for all nine controls. The [mapping screenshot](../screenshots/mixxx-service-feedback-2026-09-13.jpg) records the actual native preferences at 20:55:24.575Z. Both approved Music-library fixtures were then restored through native file dialogs and observed loaded/paused.

One unmapped `B8 77 2A` packet was sent directly to the service input by a separate local helper during diagnosis. It verified the service callback, not Mixxx feedback, and created no decoded control state. The capture retains it; it is explicitly excluded from the host-control evidence.

## Observed native behavior

| Control | Command and returned normalized state | Native UI observation |
| --- | --- | --- |
| Deck 1 volume | `B0 21 20`; 1 → 32/127 (0.2519685) | Left volume fader moved from top to approximately one quarter |
| Deck 2 volume | `B1 21 5F`; 1 → 95/127 (0.7480315) | Right volume fader moved from top to approximately three quarters |
| Crossfader | `B2 24 20`; 0.5 → 0.25 | Horizontal fader moved from center to one quarter from left |
| Deck 1 play | `B0 20 7F`; false → true | Play button orange, waveform/position advancing; captured at 4.62 seconds |
| Deck 1 repeated play | Same `B0 20 7F`; true → true | Remained playing; subsequent capture at 12.46 seconds, with no toggle off |
| Deck 1 pause | `B0 20 00`; true → false | Play button off, position held at 18.08 seconds while deck 2 played |
| Deck 2 play | `B1 20 7F`; false → true | Play button orange, position advancing; captured at 3.70 seconds |
| Deck 2 repeated play | Same `B1 20 7F`; true → true | Remained playing; subsequent capture at 14.08 seconds |
| Deck 2 pause | `B1 20 00`; true → false | Play button off, final position 20.63 seconds |

The coordinator inspected seven actual native screenshots: baseline, changed faders, both initial/repeated play states, and restored paused state. Full screenshots include local library information and remain in the private job directory; the public run records their hashes and observed control facts. The public gallery uses the controller-preferences capture without library details.

The command helper waited for independently returned fresh values rather than treating a sent result as success. It recorded 14 successful CLI requests: explicit arm, twelve setters and explicit disarm. A frozen capture prefix contains exactly twelve outgoing raw packets and twelve command-sent events, matching the test sequence. Requested and observed values remained separate. Initial/final volume readings are 1 on both decks; final crossfader is 0.5. Final play readings are false on both decks, transport open, service disarmed and capture drops zero.

## Acceptance boundary

M12 passes actual native UI, returned state and wire evidence for play/volume on both decks and the shared crossfader. Repeated desired play is not a toggle. Native fader positions corroborate returned normalized values; pixel positions do not establish physical gain calibration. These conventional messages have no request ID or actor identity, so correlation remains state-only and source attribution remains unverified. No synthetic host acceptance is claimed.

This run did not prove cue/sync, controller coexistence, B2B arbitration, latency percentiles, audio recording, musical quality or a complete AI set. Visible meters and advancing position are not a measured audible-response result. M13 and the remaining G1 checks stay open. The live service remains independently running, disarmed, with both fixtures paused and faders restored.

> End of autonomously AI-generated evidence.
