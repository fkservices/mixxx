# F08 evidence: bounded runtime message validation

> This document was autonomously generated by an AI agent at the user's request.

Status: partial handoff only; F08 is not ready for coordinator acceptance. Input integrated revision was `0ef0499db28c6c86bb7c75e390665da831f8c20c`. F01–F07 were accepted before dispatch. The action-validation child is reported separately in [F08-ACTION-VALIDATION.md](F08-ACTION-VALIDATION.md). The state-validation child must replace the unaccepted state-admission portion of [validate.ts](../../../../ai-dj/core/validate.ts) and its paired tests before F08 can be accepted.

## Result

`validateSemanticAction` admits only the closed F06 action union. It bounds string input to 65,536 UTF-8 bytes, raw values to depth 32 and 4,096 nodes, strings to 4,096 bytes, state collections to 256 entries, and action-specific arrays to their F06 limits. It requires plain JSON records, exact fields and discriminants, nonempty bounded opaque IDs, finite safe counters, valid monotonic times, action timing horizons, freshness bounds, target-deck preconditions, required ownership, cue/silent conditions, and the operation-specific absolute, relative, and lease constraints. It returns a structured error or a deep-frozen clone; it has no I/O, executor, timer, host-handle, or state mutation path.

The current state-admission code is **unaccepted and must not feed downstream work**. The F08 state-validation child must implement pure projection over a full immutable baseline; accept positive deck observations and stale old-context evidence correctly; validate profile/context, deck-binding/reading transactions, exact dynamic instance and explicit-presence resolution; and add adversarial fixtures. A shape-only validator cannot establish reconciliation.

The frozen F06 union intentionally rejects `deck.set_sync_enabled` as `unsupported-action`. F09 selected that operation after F06 closed; the coordinator will add it through an explicit versioned contract/validator extension instead of allowing an unreviewed operation name through this boundary.

## Checks

Run from `ai-dj/` on 2026-09-13:

| Command | Result |
| --- | --- |
| `npm run typecheck` | Exit 0 |
| `npm test -- test/core/validate.test.ts` | Exit 0; 9 tests passed |
| `npm run build` | Exit 0 |
| `git diff --check` from `ai-dj/` | Exit 0 |

Focused tests cover every frozen action/outcome/release-obligation variant, valid immutable copies, unsupported operation and extra script/accessor rejection, malformed IDs, absent generations, `NaN`, invalid horizons, oversized strings, recursive/deep payloads, contradictory conditions, and lease mismatch. The shallow state fixtures are temporary and do not meet F07 acceptance. These tests do not prove Mixxx, MIDI, hardware, UI, audio, timing, actual catalog discovery, or reconciliation.

## Limits and handoff

The action child is ready for coordinator review. F08 state validation remains a blocking continuation with the exact requirements above. A later reducer still owns baseline creation, replay/duplicate handling, gaps, recovery, and runtime reconciliation; a later catalog task owns real capability discovery. No action validation here executes or mutates observed state.

No manifests, action/state/capability contracts, MIDI/Mixxx/UI code, ledger, commit, push, or external communication were changed. Coordinator should review the action child, preserve the state work only as a handoff substrate, and dispatch the named state child before integrated F08 acceptance.

> End of autonomously AI-generated document.
