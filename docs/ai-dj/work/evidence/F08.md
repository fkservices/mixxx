# F08: Bounded action and observed-state admission

> Autonomously AI-generated acceptance evidence at the user's request.

Status: accepted by the coordinator after both required child tasks and integrated checks. The original partial handoff was completed through [action validation](F08-ACTION-VALIDATION.md) and [state validation](F08-STATE-VALIDATION.md). Their reports retain the individual input revisions, bounds, tests and integration boundaries. Coordinator integration was reviewed on the working tree based on `2301c95`.

## Accepted behavior

The closed F06 action/outcome/release-obligation envelopes require their exact identities, generations, arguments, resources, preconditions and lease fields. Unknown operations, arbitrary executable names, malformed data and unsafe numeric/size bounds reject before admission. Sync remains intentionally rejected until its separate versioned contract integration is accepted.

F07 snapshot/delta admission uses a bounded, immutable catalog projection and complete optional baseline, supplied by the trusted catalog/reconciler. It validates exact profile, current instances, presence, read semantics, applicable decks and actual loaded/empty bindings. Each delta is checked against its entire temporary resulting projection, including unchanged readings after a track or deck replacement. It does not mutate the caller's state or install a transaction.

Historical observations retain their own identity and age. They cannot be relabeled, refreshed or resurrected as current. Explicit stale `stream-gap` history can retain a prior stream's creation revision after a counter reset; observed values and comparable timestamps remain strictly checked. The explanatory clarification is recorded in [the state contract](../contracts/state.md).

The root review also required independent byte/node budgets for each of the three reference components, so a legal bounded baseline does not become invalid merely because its catalog adds bytes. Wire messages and full projected snapshots retain the 65,536-byte ceiling. Duplicate JSON keys and inherited array serialization hooks reject without executing payload hooks.

## Integrated verification

On 2026-09-13, after the state worker's final handoff, the coordinator independently ran `npm test`, `npm run typecheck`, and `npm run build` from `ai-dj/`. All exited 0. The complete suite passed 39 tests: 28 action/state validator tests, six diagnostics tests, four MIDI-port discovery tests and one scaffold smoke test. Both child reports and complete changed source/test areas were reviewed. The independent worker deadline monitor was terminated after all handoffs, within the original deadline.

These are synthetic admission and local package checks. Actual catalog discovery, evidence-store trust, long-term duplicate/replay classification, gap recovery, installing accepted state, dispatch-time freshness/authority and native MIDI/UI/audio behavior remain the respective runtime tasks. F08 acceptance does not convert an intended or sent action into an observed host result.

> End of autonomously AI-generated evidence.
