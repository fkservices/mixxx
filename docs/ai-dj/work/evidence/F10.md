# F10 evidence: inventory format and pagination

> Autonomously AI-generated evidence at the user's request.

Implemented against integrated `0ef0499db28c6c86bb7c75e390665da831f8c20c` with F07 accepted. The requested Sol worker could not be created because the current agent-thread limit was reached. The coordinator completed this bounded task directly while three workers owned disjoint F08/F09/F12 files; no Sol execution is claimed. F10 owns only its inventory schema, README and this report.

## Deliverables

- [Schema](../inventory/schema.json): strict Draft 2020-12 discovery pages with a maximum of 20 rows, exact F07 context/profile identity, all 22 families, canonical and alias records, explicit unresolved/extension-needed states, dynamic instance incarnations and separate availability/command/feedback/native UI/runtime assessments. Source cursors, continuation records and four-source discovery closure retain gaps and distinguish inventory completeness from feature implementation.
- [Inventory contract and reproducible fixtures](../inventory/README.md): pagination, denominator, revision/alias/instance and evidence rules, external parse limits, capacity behavior and the semantic checks that JSON Schema cannot enforce. Includes ordinary control, dynamic effect, modal service, closure, 20-row boundary and alias examples. Every example is synthetic.

The format includes descriptive group/key or bounded operation-spec references, never a generic executable script field. A future reviewed converter must resolve those specifications into the closed action contract. Readable values preserve units/range/mapping references; write specifications reference preconditions, postconditions, deadlines, retry and mapping rules. Unresolved mappings cannot execute merely because the schema validates.

## Checks and results

Executed the README's Python block directly from the repository root using installed Python 3.10 and `jsonschema` 4.26.0. `Draft202012Validator.check_schema` passed. Final output:

```text
PASS: 6 valid examples, 12 schema rejections, 5 required semantic rejection examples
```

Valid examples covered all three required record kinds plus a synthetic exhausted discovery page, exactly 20 rows, and an alias page. Rejections covered 21 rows, missing connection generation, extra executable field, empty canonical suffix, null continuation cursor, empty passed evidence, malformed/invalid-calendar timestamps, empty discovery evidence, newline-suffixed IDs, incomplete closure and repeated closure cross-check kinds.

The five semantic examples are deliberately schema-valid yet require rejection by a collector/reviewer: wrong row family, duplicate canonical ID, mismatched complete instance count, foreign evidence context and incorrect closure total. These assertions demonstrate the boundary; they are not a production closure validator or evidence resolver.

An initial fixture exposed that optional `date-time` validation was absent from the installed FormatChecker. The final fixture explicitly registers calendar-valid UTC checking and the schema requires UTC timestamp syntax. A Python 3.10 parsing difference was corrected by normalizing the `Z` suffix to `+00:00`. Both malformed syntax and an impossible calendar date now fail. No dependency installation or package change was needed.

`npx --yes markdownlint-cli2@0.23.2 docs/ai-dj/work/inventory/README.md` and `git diff --check` exited 0. Schema IDs/family names and context/instance semantics were compared directly with accepted F06/F07 sources and the 22-family coverage plan. The schema and README preserve the distinction between discovery closure and C02 implementation/runtime closure.

## Evidence limits

No Mixxx launch, source inventory scan, MIDI endpoint, UI action, audio test, real capability assessment or hardware support claim was made. Future collectors must enforce pre-parse bounds, exact references, page-chain/cursor continuity, denominator reconciliation, dynamic parent generations, source/manual/menu/runtime exhaustion and real retained evidence. Capacity limits require explicit continuation or a protocol extension, never truncation and a false closure.

Parent graph Verify evidence: project `mixxx-ai-dj`, generation `2026-09-13T16:44:03Z`, ready with 47,976 nodes and 238,220 edges. `ai-dj/*` returned zero matches with no more pages. The relevant new inventory paths were missing/untracked and accepted core paths had unavailable coverage. Direct complete contract/source reads supplied the fallback. No graph completeness claim was used.

> End of autonomously AI-generated evidence.
