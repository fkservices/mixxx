# U01 — Desktop screens and local service API

> Autonomously AI-generated contract evidence at the user's request.

Source base: `57203c4`. [Desktop contract](../contracts/ui.md) defines browser-native TypeScript/HTML views and a persistent loopback Node service using the existing compiler. It covers setup, source/set editing, musical review, live booth, history and settings; no new dependency was installed.

The proposed API separates state snapshots, normal commands, priority Disarm, authenticated event subscriptions and paged history. It specifies request identity, expected revisions, receipt-versus-result display, origin/Host checks, one-use launcher bootstrap, bounded per-client queues and resynchronization. No endpoint implementation is claimed. History has no performance dispatch path; closing a UI tab is distinct from service/host failure.

## Contract validation

Reviewed the import → arrange → review → start → perform → history path for AI Only, B2B and Playlist Only against the accepted P01/O01/R15 contracts. The explicit matrix also covers impossible duration, armed mode changes, historical inspection during live playback, lost connectivity and restart. Disarm bypasses planner and normal command backlog, does not require a matching stale UI revision, and is never shown successful without confirmation. Playlist Only cannot arm/load/mix; history cannot mint commands. B2B holds bass for the actual current transition and requires a later transition plus release/quiet and fresh state. Human track replacement retains the displaced occurrence.

Screen coverage was checked against SESSION-UI and delegated defaults: actor/stage filters, raw inspector, distinct duplicate occurrences, actual spans, capture gaps, unknown authorship, AI/user musical overrides, custom energy curves, existing cues, notes/ratings, preference correction, retention, audio opt-in, waveform provenance and keyboard access all have an owner. The contract links follow-up runtime tasks explicitly.

Planning artifact validation and generated-file consistency checks passed. This is design review, not browser, security, MIDI latency or runtime acceptance. No new executable tests were needed for this documentation-only milestone. Graph metadata for the exact documentation/package paths was unavailable at generation `2026-09-13T16:44:03Z`; current files supplied the source evidence. The historical memory note was used only to locate the project; current repository state supersedes it.

> End of autonomously AI-generated contract evidence.
