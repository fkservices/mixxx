# F07 evidence: observed state and capability contracts

> This document was autonomously generated by an AI agent at the user's request.

Worker result: ready for coordinator review; not self-accepted. Dispatched against integrated `b5dc433`, with accepted F06 action contracts. F04 completed during this assignment; compiler/build validation ran after the coordinator confirmed stable manifests at `aff2144`. Only the four assigned F07 files were written. No Mixxx launch, device/MIDI/audio changes, upstream communication, commits, manifests, action files or executor/validator implementation were performed.

## Deliverables and semantic review

- `ai-dj/core/state.ts`: data-only observed/stale/missing/unknown/uncertain union; F06 identity/attribution imports; deck binding context; capability readings; full replacement snapshots; deltas with exact base/revision/ordering; gap recovery records; separate requested state and inferred intent.
- `ai-dj/core/capabilities.ts`: canonical definitions and separate aliases; all 22 family identifiers; version/build/platform/skin/configuration and dynamic instance generations; read/write semantics; unresolved and extension-needed states; independent presence plus command/feedback/native UI/runtime assessments. Catalog metadata is not runtime proof.
- `docs/ai-dj/work/contracts/state.md`: normative numeric/identity/freshness rules, canonical and dynamic denominator accounting, snapshot/delta admission and replacement semantics, missing-zero/alias/manual-control examples, and F08 validation handoff.
- This evidence document records checks and limits. The coordinator owns acceptance and ledger changes.

Required examples reviewed directly against the types and F06:

1. **Missing zero:** unverified zero is unknown/existence-unverified, with no value. Explicit absence yields missing with presence evidence, never observed false/off. A current zero requires presence, mapping, binding and freshness evidence.
2. **Aliases:** two alias IDs target one canonical capability and contribute zero extra denominator entries. Dynamic deck/effect instances require their own evidence; incomplete enumeration cannot report full coverage.
3. **Snapshot/delta:** snapshot 40/revision 100, exact-base delta 41/101, gap at 43/base 102, replacement snapshot 44/104; old baseline/context cannot mutate the new one. Requested/sent play never replaces actual playing state. Same-file reload still changes track generation.
4. **Manual change:** actual feedback volume 0.4 remains unknown-author; a separate confidence-bearing interpretation may suggest human intervention. Source input alone does not prove host effect because pickup may suppress it. Human attribution requires source and linked evidence identities; policy inference does not overwrite observed state.

These are contract examples, not executed host scenarios or a tested reducer.

## Checks

| Check | Result |
| --- | --- |
| `npm run typecheck` from `ai-dj/`, stable `aff2144` package plus F07 working files | Exit 0. |
| `npm run build` from `ai-dj/`, same inputs | Exit 0. |
| Inspect generated `dist/core/state.js` and `dist/core/capabilities.js` | Each contains only disclosure comments and `export {};`; type imports erased and no runtime/native imports or handles. Generated output remains ignored. |
| Temporary standalone compile fixture using copied F06/F07 sources | Exit 0 under NodeNext, strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, verbatimModuleSyntax, erasableSyntaxOnly, allowImportingTsExtensions, ES2024. |
| Compile fixture positive shapes | Unknown-zero, alias records, capability semantics, full snapshot, base-linked delta, requested state, feedback with unknown attribution, and independent assessment dimensions compile. |
| Compile fixture negative cases using consumed `@ts-expect-error` | Unknown observation carrying false, delta lacking base/previous sequence, requested state assigned as an observation, and human attribution lacking source/evidence are rejected by the compiler. |
| Direct review of complete new TypeScript source and accepted action source/contract | Types reuse existing identities; only core type imports; no validator/reconciler/algorithm implementation. |
| `git diff --check` | Exit 0 on the shared working tree at check time. |

Temporary compile fixtures are scratch verification only, outside the repository. They prove TypeScript shape checks, not runtime parsing or cross-field validation. F08 must implement those checks and choose tested payload limits; later cards own actual state/ordering reconciliation and capability discovery. No runtime test suite was added for declarations alone.

## Graph coverage and remaining limits

Used codebase-memory Verify tier. Parent supplied graph discovery for `ai-dj/*`: zero matches, no next page. Worker independently confirmed project `mixxx-ai-dj` at this workspace, ready with 47,976 nodes and 238,220 edges. Coverage generation remains `2026-09-13T16:44:03Z`.

Exact coverage check for new state/capability source, accepted actions source/contract, layout and coverage plan returned `coverage_unavailable` / `not_tracked`; the not-yet-created state document returned missing at that check. Bounded `ai-dj/core/` scope returned zero entries, no more pages and unavailable coverage. The graph therefore supplies no structural completeness proof. Complete direct source reads provided the necessary fallback for the owned contracts and accepted dependency. No Mixxx implementation claim is inferred from these graph results.

Open implementation work is intentionally assigned to F08 and later cards: bounded JSON parsing, state reconciliation, raw-input ordering, presence discovery, complete inventories, native/UI/runtime evidence, B2B holds, arming/cancellation and runtime behavior. This is not evidence that any capability works in Mixxx. No F07 continuation is required for the bounded schema deliverable; coordinator review/integrated acceptance remains outstanding.

> End of autonomously AI-generated document.
