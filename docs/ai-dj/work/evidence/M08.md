# M08: raw sync setter and host-value sample

> Autonomously AI-generated implementation evidence at the user's request.

Input revision `7eedc57`; M05 and the F09 sync contract/validator/profile integrations accepted. Coordinator implementation; no runtime lease or installed mapping change.

[Fragment](../../../../ai-dj/hosts/mixxx/fragments/sync.js) accepts only the fixed B0/B1 address 22 (hex) routes with matching deck channel/group and integer data 0–127. It writes `sync_enabled` as a desired boolean, with no chosen leader, pulse, clock output, automatic release, retry, or compensating shutdown write. Connections probe only the two profile-declared controls; missing connections suppress writes.

`AIDJ.readSyncState(1|2)` reads the host control independently of command intent. It returns a local control-value sample only for exact 0/1; unavailable connections, inactive lifecycle, unsupported decks, invalid values and read exceptions remain unknown. The result carries unknown actor and no correlation. It contains no request acknowledgement, timing, leader, phase, BPM, track identity or musical completion assertion. No command cache supplies observation values.

[Five tests](../../../../ai-dj/test/host/sync.test.ts) cover both decks, zero/mid/max and repeated requests, host-rejected/unapplied requests with independent state changes, rapid explicit on/off, no reinit/shutdown writes, unknown/invalid/read-failure observations, unavailable control handling, and rejection of MIDI clock, note edges, unrelated addresses, group/channel mismatches and malformed data.

| Local check | Result |
| --- | --- |
| `npm test` | 85 passed |
| `npm run typecheck` | Passed |
| `npm run build` | Passed |

These are stub-level implementation tests. The rapid pair test verifies exact emitted host calls, not whether an audio callback has applied a one-shot sync or undone its effects. F09's accepted released-source analysis remains the source of those host semantics. New-file graph coverage was missing in generation `2026-09-13T16:44:03Z`; direct contract/source inspection supplied the evidence.

M09 owns conventional feedback emission; M10 assembles the fragments. Real sync behavior, rejected enable, phase/tempo effects, leader changes, native UI and latency remain live-test work. Production semantic sync still requires the complete configured topology and `mixxx.sync-guard.v1`; the two mapped decks and this local boolean sample cannot satisfy that guard. The semantic encoder continues returning extension-needed.

> End of autonomously AI-generated implementation evidence.
