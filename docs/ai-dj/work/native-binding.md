# Native MIDI binding: F05 verification

> This document was autonomously generated by an AI agent at the user's request.

F05 verified that the isolated `ai-dj` package can load `@julusian/midi@3.8.1` and safely enumerate the local CoreMIDI endpoint inventory on the current integrated revision `aff2144740fa52955bd30c10618360fb73d433d6`.

## Recorded runtime and package identity

The prior terminal install record is [native-install.json](jobs/native-install.json). It records F04's exact install and script-policy result; F05 did not reinstall or alter its manifest, lockfile, or dependency policy.

F05's read-only identity check from `ai-dj/` reported:

```text
@mixxx/ai-dj@0.0.0 /Users/faizkhalid/Documents/ChatGPT/DJ_app/ai-dj
└── @julusian/midi@3.8.1

Node v26.8.1
npm 11.19.0
macOS 27.0 (build 26A5416b)
arm64
```

The installed Darwin arm64 N-API addon remains `node_modules/@julusian/midi/prebuilds/midi-darwin-arm64/node-napi-v7.node`, a Mach-O 64-bit arm64 bundle with SHA-256 `15d820a8c6bc69ae6ccf32b6cf1aefd07b4c6f7e25488414b463bdf0215df9f1`.

## Native-load and discovery result

The package README describes `@julusian/midi` as a CoreMIDI-capable RtMidi wrapper and states that importing its main entrypoint loads the native addon immediately. Its installed main module invokes that verification at import time. F05 imported the main entrypoint under the frozen runtime, constructed one `Input` and one `Output`, read each count and indexed names, checked that neither handle was open, then called `destroy()` on both in a `finally` block.

The command exited `0`; the full command and terminal result are preserved in [F05 evidence](evidence/F05.md). Native import succeeded. Actual endpoint enumeration at `2026-09-13T19:04:35Z` was:

| Direction | Count | Names | `isPortOpen()` before cleanup |
| --- | ---: | --- | --- |
| Input | 0 | none | `false` |
| Output | 0 | none | `false` |

Both `destroy()` calls completed after enumeration. The command never called `openPort`, `openVirtualPort`, `sendMessage`, or a stream helper, so it did not open/create a MIDI port or send MIDI data.

## Scope and limitations

This is positive evidence that the selected package's arm64 native addon loads and can enumerate the current machine under Node `v26.8.1`. It does not prove a physical controller is connected, enumerate later-connected hardware, select a route, establish IAC buses, open a MIDI port, exchange messages, demonstrate routing, or show Mixxx interoperability. The expected later Hercules DJControl Mix Ultra was not connected during this check; the zero-endpoint result is inventory evidence, not a native-load failure. M01 and later owned runtime tasks must repeat discovery/selection and prove their own lifecycle behavior when their dependencies and device prerequisites are met.

No native-load error occurred, so F05 has no triggered FIX proposal.

> End of autonomously AI-generated document.
