# F01 local test baseline

> This document was autonomously generated by an AI agent at the user's request.

Status: selected documentation baseline, recorded 2026-09-13 on source revision `4067eb8e1990f5d6fec991f6d4b2a9b191a51abe`. No Mixxx executable was installed or launched, no profile was created, no MIDI or audio device was reconfigured, and no track was played.

## Observed facts

These facts describe the machine at inspection time. They are not runtime or musical evidence.

| Item | Observed value | Evidence boundary |
| --- | --- | --- |
| Checkout | Branch `codex/ai-dj-plan`; commit `4067eb8e1990f5d6fec991f6d4b2a9b191a51abe` | `git rev-parse HEAD` and `git branch --show-current` |
| Inspected source | `CMakeLists.txt` declares Mixxx `2.7.0-alpha` | Source metadata only; there is no built executable from this checkout |
| macOS | macOS 27.0, build `26A5416b` | `sw_vers` |
| Machine | Apple M1 Pro, `arm64`, model identifier `MacBookPro18,3`, 8 cores (6 performance, 2 efficiency) | No serial number was collected |
| Physical memory | 34,359,738,368 bytes (32 GiB) | Installed memory from `sysctl -n hw.memsize`; this is not current free memory |
| Node toolchain | Node `v26.8.1` at `/Users/faizkhalid/.local/bin/node`; npm `11.19.0` at `/Users/faizkhalid/.local/bin/npm` | Availability only; native MIDI compatibility is untested |
| Other tools | Apple clang `17.0.0`; Homebrew `6.0.21`; Python `3.10.7`; Git `2.52.0` | `cmake` and `ninja` were not on `PATH` |
| Default audio | MacBook Pro Speakers, 2 output channels, 48,000 Hz; MacBook Pro Microphone was the default input at 48,000 Hz | Device enumeration only; Mixxx audio settings and buffer size are unobserved |
| MIDI enumeration | `system_profiler SPMIDIDataType` returned no device records | This does not prove that a CoreMIDI or IAC endpoint cannot be created later |
| Mixxx application | Not installed at `/Applications/Mixxx.app` or `/Users/faizkhalid/Applications/Mixxx.app`; no `mixxx` executable was on `PATH`; the Homebrew `mixxx` cask was absent | Therefore no installed version metadata, UI, audio, or controller behavior was observed |
| Existing Mixxx profiles | Neither the normal `/Users/faizkhalid/Library/Application Support/Mixxx` location nor the selected disposable profile existed | No production profile was opened or changed |
| Audio files | The repository includes short test assets such as `src/test/sine-30.wav` and stem/tag fixtures | They are software test assets, not operator-approved musical fixtures and cannot satisfy F11 listening evidence |

## Selected initial baseline

The following values are choices for the first runtime fixture. They remain unobserved until the prerequisites below are completed.

| Decision | Selected value | Reason and constraint |
| --- | --- | --- |
| Mixxx runtime | **Mixxx 2.5.6 stable, macOS ARM build** | The [official download page](https://mixxx.org/download/) lists 2.5.6 as the current stable release and recommends it for live use. The selected artifact's published SHA-256 must be captured by the installation job before use. |
| Documentation profile | Mixxx 2.5 manual and 2.5 controls reference | Runtime controls must be checked against the selected stable version, not inferred from the 2.7-alpha checkout. |
| Skin | **LateNight**, **PaleMoon** color theme, legacy XML UI path, two decks | The [Mixxx 2.5 user-interface manual](https://manual.mixxx.org/2.5/en/chapters/user_interface) documents this skin and theme. This is the legacy path traced by the current plan; QML is a separate profile. |
| Disposable profile ID | `mixxx-2.5.6-latenight-f01` | This ID must be included in timing and runtime artifacts. |
| Disposable profile path | `/Users/faizkhalid/Library/Application Support/Mixxx-AI-DJ/profiles/mixxx-2.5.6-latenight-f01` | It is outside Git and separate from Mixxx's normal profile. Future launch must pass this exact path with `--settings-path`; the source exposes that option in `src/util/cmdlineargs.cpp`. Never substitute the normal profile. |
| Initial service runtime | Installed Node `v26.8.1` and npm `11.19.0` | This freezes the first native-binding attempt. F04/F05 must prove compatibility; a different Node version requires an explicit baseline update with its reason and evidence. |
| Audio output | MacBook Pro Speakers at 48,000 Hz, low operator-approved level | F11 must verify the device is still present and bind the actual Mixxx buffer size before playback. M18 must bind a capture route separately; this output selection is not capture evidence. |
| MIDI routes | Two native virtual endpoints with shared logical name `AI DJ`, separate command Output and feedback Input | Coordinator correction after F04: see [port topology](contracts/ports.md). Supersedes the initial unverified IAC bus choice. F11 owns actual pairing and echo proof. |
| Host scope | Same Mac: local TypeScript/Node service, local planner process, Mixxx desktop, and browser UI on loopback | No other DJ host, external AI computer, Raspberry Pi, LAN control endpoint, or QML profile is part of this baseline. |

The future launch shape is:

```text
/Applications/Mixxx.app/Contents/MacOS/Mixxx --settings-path "/Users/faizkhalid/Library/Application Support/Mixxx-AI-DJ/profiles/mixxx-2.5.6-latenight-f01"
```

This command was not run in F01. Its executable casing was corrected after the [installation read-back](setup/mixxx-runtime.md). The [fixture record](setup/music-fixtures.md) and [controller note](setup/controller.md) capture later user inputs and setup results.

## Fixture track contract

No operator-approved track identities or paths were provided to F01, so neither track slot is satisfied. F11 requires both before it can start:

| Slot | Required operator input | F11 record |
| --- | --- | --- |
| `fixture-track-a` | One legally accessible, full-length local music file approved for playback and test recording | Stable occurrence ID; absolute local path kept out of Git; size; duration; codec; SHA-256; analyzed BPM/key/confidence; cue availability |
| `fixture-track-b` | A second approved full-length local music file, preferably with a useful tempo/key/energy contrast for a real transition | The same identity fields, plus the intended transition stress case relative to track A |

The files stay in their operator-owned local locations. Git artifacts may store redacted labels and fingerprints, but must not copy music or publish unnecessary absolute paths. Repository test WAV/MP3 files do not replace these inputs, and filenames must not be guessed.

## Timing and capacity gates selected before measurement

These are acceptance targets, not observed performance:

- Command send to confirmed host observation: p95 at most 30 ms and p99 at most 75 ms.
- Recognized human input to AI cancellation: p95 at most 20 ms and p99 at most 50 ms.
- Observed host state to the eventual app display: p95 at most 100 ms. This remains unmeasured until U08/L05; M19 must not infer it from logs.
- Native Mixxx UI response: each of M17's 20 simple-control changes should appear within 150 ms after the timestamped stimulus, with the camera/frame uncertainty reported.
- Beat-critical host-quantized gate: at 120 BPM, request a supported quantized cue/play/sync action at least one beat before its target. Absolute host-boundary error must be p95 at most 30 ms and p99 at most 75 ms, with no missed target by one quarter beat or more (125 ms). Audible onset relative to the same target must be p95 at most 50 ms and p99 at most 100 ms. Report every miss; do not silently loosen these values after a failure.
- Capacity envelope: 1–250 playlist occurrences and approximately 10 minutes–6 hours, including duplicates and open-format material. This is a validation envelope, not a measured capacity or product limit. Later release evidence requires a separate uninterrupted 30-minute set and full 6-hour endurance job.

M15–M19 must use these measurement requirements:

1. Keep wall time only for artifact correlation. Use a monotonic Node clock for service send, receive, deadline, and cancellation timestamps; record the runtime and timer API.
2. Treat CoreMIDI timestamps, Mixxx host/beat position, video frames, and captured-audio samples as separate clock domains. Calibrate offsets before and after each run and report drift plus uncertainty. Never label a round trip as one-way latency.
3. M15 runs loopback calibration and command-to-confirmed-host-state sampling as separate phases, retains raw samples, and reports count, p50/p95/p99, maximum, missing confirmations, expired deadlines, and dispatch-interval error.
4. M16 repeats the identical stimulus with a documented synthetic analysis load in a separate process and adds process CPU/memory, Node event-loop delay, Mixxx overload counters, and any catch-up bursts.
5. M17 records the real LateNight UI for 20 returned-state-correlated changes. Its uncertainty floor is at least half a video frame plus clock-alignment error.
6. M18 places command markers and output audio in an aligned capture, exercises cue/play/sync repeatedly in one short run, reports the audio sample rate/buffer/route, and separates host observation, visible UI, and audible onset.
7. M19 accepts, requests more evidence, or creates bounded `SCHED` correction children. Averages alone cannot pass a percentile gate; any uncertainty interval crossing a limit is inconclusive rather than a pass.

## Prerequisites and owners

These proposed setup children are recorded here for coordinator materialization; F01 does not modify the catalog.

| Proposed child / existing owner | Required result | Blocks |
| --- | --- | --- |
| `SETUP-F01-MIXXX-2.5.6-ARM` — coordinator-owned durable install/download job | Obtain the official ARM artifact, verify its published SHA-256, install it without opening a normal profile, and read back `CFBundleShortVersionString`/`CFBundleVersion` | F11 |
| `SETUP-F01-FIXTURE-TRACKS` — operator input, recorded by the coordinator | Approve exactly two local full-length fixture tracks under the contract above; do not copy them into Git | F11 |
| F11 | Create only the selected disposable profile, select LateNight/PaleMoon and two decks, confirm safe 48 kHz output and actual buffer, create/pair the two native virtual routes, load both fixtures, and capture baseline UI/audio/overload facts | All live runtime inventory |
| F04/F05 | Prove the selected Node version can install/import the chosen native MIDI binding, or propose and document a version correction | MIDI implementation |
| O04/R23 | Identify the exact physical controller model, mapping, ports, and pickup behavior before controller coexistence evidence | Real-controller/B2B acceptance; not F11's controller-free fixture setup |
| M18 | Bind and calibrate an output-audio capture route suitable for audible timing | Audible timing review |

F02 is the next eligible card after coordinator review of this documentation baseline. F11 remains blocked on the Mixxx installation and two operator-approved fixture tracks even if F01 is accepted.

> End of autonomously AI-generated document.
