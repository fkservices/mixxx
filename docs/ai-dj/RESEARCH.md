# Mixxx source and documentation findings

> This document was autonomously generated by an AI agent at the user's request. It is a planning draft for human review.

Inspected on 2026-09-13. Upstream source pinned to `3e5f59002cdd02040f3b7aeef938ea9af2def001`. Conclusions below are source-level findings unless explicitly stated otherwise.

## Repository and version

The fork was created as [fkservices/mixxx](https://github.com/fkservices/mixxx) and its GitHub metadata verified `isFork: true` with parent `mixxxdj/mixxx`. The empty local workspace was populated from upstream `main`, with `origin` pointing to the fork and `upstream` pointing to Mixxx. The local planning branch is `codex/ai-dj-plan`.

[CMakeLists.txt](../../CMakeLists.txt) lines 510–512 identify the source as 2.7.0-alpha; line 575 selects C++20. [CONTRIBUTING.md](../../CONTRIBUTING.md) describes the C++/Qt application, JavaScript controller mappings, CMake build workflow, and evolving QML UI. The official [download page](https://mixxx.org/download/) lists 2.5.6 stable, 2.6 beta, and 2.7 development at inspection time. A plan referencing `main` is not proof of compatibility with stable Mixxx.

## Source evidence

Paths below are relative to the pinned source checkout. Line ranges describe the inspected baseline; they may move in future revisions.

| Finding | Source | Implication |
| --- | --- | --- |
| MIDI input resolves mapped controls and dispatches scripting handlers. | [midicontroller.cpp](../../src/controllers/midi/midicontroller.cpp), 274–321 and 323–425 | A custom mapping can translate an external DJ device's MIDI into host actions. |
| The legacy short-message handler returns immediately for `0xF8`. | [midicontroller.cpp](../../src/controllers/midi/midicontroller.cpp), 282–291 | Do not rely on inbound MIDI beat-clock synchronization on this path. This is a scoped finding, not a claim about every possible Mixxx synchronization mechanism. |
| Paired 14-bit mapping flags are handled with queued partial messages. | [midicontroller.cpp](../../src/controllers/midi/midicontroller.cpp), 388–425 | Higher-resolution control is possible, but pairing/order/error behavior must be tested. |
| SysEx input dispatches to the script engine. | [midicontroller.cpp](../../src/controllers/midi/midicontroller.cpp), 577–621 | Custom bidirectional automation messages can be carried over MIDI; their protocol remains to be implemented. |
| MIDI JavaScript proxy exposes short and SysEx send methods. | [midicontroller.h](../../src/controllers/midi/midicontroller.h), 135–152; [MIDI declarations](../../res/controllers/midi-controller-api.d.ts), 7–34 | Feedback can use the same transport class as commands. |
| Script setters resolve a ControlObjectScript and respect soft-takeover filtering. | [controllerscriptinterfacelegacy.cpp](../../src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp), 143–205 | A setter call is a request, not proof that the requested value was applied. |
| Script connections support buffered and superseded-value behavior. | [controllerscriptinterfacelegacy.cpp](../../src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp), 262–320 | Subscribe to actual state changes; choose telemetry behavior according to its loss tolerance. |
| Shared control setting may emit a confirmation request; unchanged values may suppress events. | [control.cpp](../../src/control/control.cpp), 269–311 | Explicit reconciliation is required for asynchronous and already-satisfied commands. |
| Widgets connect to shared control changes and update the connected widget/property. | [controlwidgetconnection.cpp](../../src/widget/controlwidgetconnection.cpp), 35–53, 98–103 and 153–196 | Native UI feedback follows the shared control route for these bindings. A visible runtime test is still required. |
| `engine.getPlayer` is declared and forwards to the script engine. | [engine-api.d.ts](../../res/controllers/engine-api.d.ts), 40–133; [controllerscriptinterfacelegacy.cpp](../../src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp), 139–141 | Current source includes loaded-player metadata access; do not assume older-version support. |
| Player proxy exposes read-only metadata properties and change signals. | [javascriptplayerproxy.h](../../src/controllers/scripting/javascriptplayerproxy.h), 15–62; [implementation](../../src/controllers/scripting/javascriptplayerproxy.cpp), 7–147 | Useful for feedback. The inspected proxy is not a general library browsing/loading interface or unique track identifier. |
| Selected-track loading is backed by the current library table. | [librarycontrol.cpp](../../src/library/librarycontrol.cpp), 28–87 and 671–689 | UI row selection is not a reliable substitute for a stable catalog ID. |
| Repeated load actions can invoke clone-on-double-tap behavior. | [playermanager.cpp](../../src/mixer/playermanager.cpp), 611–691 | Blind load retries can do something different from the original request. |
| PlayerManager routes location loading through the library. | [playermanager.cpp](../../src/mixer/playermanager.cpp), 694–718 | A narrow future loading extension should reuse host services and their policies. |
| Common script helper triggers a control then resets it by timer. | [common-controller-scripts.js](../../res/controllers/common-controller-scripts.js), 258–271 | Momentary actions need release semantics and recovery, not just a value of 1. |
| Generated declarations distinguish writable and read-only controls and list dynamic group families. | [mixxx-controls.d.ts](../../res/controllers/mixxx-controls.d.ts), 1–174 | Seed a versioned capability inventory; don't blindly expose every name as writable. |
| Sampler bank controls open file dialogs; recording has distinct initialization/active states. | [mixxx-controls.d.ts](../../res/controllers/mixxx-controls.d.ts), 3683–3723 | A MIDI trigger is not completion of a whole application workflow. |

## Graph verification and limitations

Used the codebase-memory skill at Tier 2 (task-directed verification). No Mixxx project was indexed initially; the repository was indexed as `mixxx-ai-dj` in fast mode. Generation: `2026-09-13T16:44:03Z`. The index reported 47,976 nodes, 238,220 edges, 919 partially parsed files, and 2,176 deliberately excluded files. No completeness claim is based on those counts.

Graph searches located the MIDI controller, scripting setters, widget connections, player metadata, library control, and player manager. Broad orientation queries were narrowed to exact files and symbols; relevant exact-file queries returned complete pages. Bidirectional one-hop traces were inspected for the script setter and widget update callback. Dynamic JavaScript/Qt relationships were not fully represented; a setter trace also included an implausible unrelated `arg` edge, which was not used as evidence.

`check_index_coverage` checked all 17 repository evidence paths. Relevant C++ files had parse gaps, and all three `.d.ts` files were excluded by the fast index. Direct source reads and targeted searches supplied evidence for those files, including partial MIDI dispatch, Qt widget updates, metadata macros/properties, and library loading code. The entire library-control file was marked partial; the bounded loading/navigation claims above were checked directly. This was not an exhaustive source audit or a trace of every engine/UI path.

There was no application dependency setup, build, mapping execution, MIDI round trip, audio recording, UI automation, Pi test, or comparison against proprietary host behavior. The plan deliberately labels runtime budgets and protocol messages as proposals. Documentation was checked using the repository-pinned Markdown linter through `npx`, plus local-link and text checks.

## External documentation

- [Mixxx 2.7 control reference](https://manual.mixxx.org/2.7/en/chapters/appendix/mixxx_controls): shared control groups/keys and developer-mode inspection. Development documentation must match the chosen runtime.
- [Mixxx 2.5 control reference](https://manual.mixxx.org/2.5/en/chapters/appendix/mixxx_controls.html): stable-version comparison for the initial common-control profile.
- [Controller scripting](https://github.com/mixxxdj/mixxx/wiki/MIDI-Scripting): normalized parameter setters and callbacks for controller feedback.
- [MIDI mapping format](https://github.com/mixxxdj/mixxx/wiki/MIDI-Controller-Mapping-File-Format): mapping configuration reference for the future implementation.
- [Mixxx download/version status](https://mixxx.org/download/): stable versus development release baseline.
- [Linux MIDI gadget testing](https://docs.kernel.org/usb/gadget-testing.html#using-midi-function): kernel MIDI gadget functionality and two-direction testing, for the later hardware milestone.
- [Raspberry Pi gadget-mode hardware notes](https://www.raspberrypi.com/news/usb-gadget-mode-in-raspberry-pi-os-ssh-over-usb/): OTG-capable ports and power considerations. This page's networking gadget configuration is not a MIDI setup recipe.

The Linux compile wiki surfaced legacy SCons guidance alongside current material. Follow the pinned repository's CMake instructions when implementation begins; do not copy old Raspberry Pi build flags without verification.

## Contribution handling

The inherited [AGENTS.md](../../AGENTS.md) permits AI-assisted preparation and reserves commits, pushes, and pull requests for deliberate human action. The user subsequently explicitly requested committing and pushing these documents to the fork. AI-generated documentation is identified at its start and end as required. No upstream issue or pull request was created.

> End of autonomously AI-generated planning document.
